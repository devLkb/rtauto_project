# 비전(텔레옵) + ML-Agents 공용 가상환경 — Python 3.10.11에서 검증
# (mlagents는 패치버전을 안 가리지만, Windows installer가 배포되는 마지막 3.10 패치가
# 3.10.11이라 이걸로 고정 — 2026-08-26. 상세: docs/PYTHON_ENV_SETUP.md)
# 2026-07-14 현재 vision/.vision 실제 설치 버전 기준.
# 핵심 변경: mediapipe 0.10.11은 protobuf 3.20.3에서 동작하므로
# mlagents와 같은 가상환경에 공존 가능하다. 더 올리면 protobuf 4.x 충돌 가능.
# 전체 잠금/감사용 스냅샷: vision/requirements-vision-mlagents.*.txt
-c vision/requirements-vision-mlagents.constraints.txt

mediapipe==0.10.11
opencv-contrib-python==4.8.1.78
numpy==1.23.5
protobuf==3.20.3
pandas==2.0.3
scipy==1.10.1

# UR16e RTDE (arm/ur_rtde_bridge.py) — URSim/실물 팔 디지털 트윈. mediapipe/mlagents와
# 별개 기능이라 버전 충돌 이력 없음. Windows/Linux 모두 PyPI 휠 제공.
ur_rtde==1.6.5

# vision/dg5f/dg5f_sdk_bridge_dgsdk.py(공식 dgsdk 래퍼, 2026-09-14 — 미검증/드라이런 전용)가
# 요구하는 유일한 외부 의존성. 벤더 패키지 자체(vision/dg5f/vendor/dgsdk-python/)는
# 저장소에 vendor로 커밋돼 있어 별도 설치가 필요 없다.
cffi>=1.15.0

# D405(RealSense) 실측·점 덩어리용. 2026-09-18 추가 — 카메라가 도착해 실제로 쓰기 시작했다.
pyrealsense2==2.58.4.10922

# 물체 찾기 (vision/d405/yolo_assist.py). YOLOE 는 글자로 찾을 것을 정하는 방식이라
# 정해진 80종에 안 묶인다 — "처음 보는 물체" 목적에 맞는다.
# ⚠️ 설치할 때 torch 를 갈아치우려 든다. **torch 는 위 핀을 유지할 것**
#    (2026-09-18 에 2.1.1+cu121 -> 2.14.0+cpu 로 바뀌어 CUDA 가 꺼졌다. 되돌렸다)
#    설치 후 확인: python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
# ⚠️ opencv 도 갈아치우려 든다. ultralytics 는 opencv-python 을 끌고 들어오는데, 이것은
#    위에 고정한 opencv-contrib-python 과 **같은 폴더에 같은 이름으로** 깔려 서로를 덮어쓴다
#    (2026-09-18 리눅스에서 4.8.1 -> 4.11.0 으로 바뀌었다. 되돌렸다).
#    설치 후 확인: python -c "import cv2; print(cv2.__version__)"   # 4.8.1 이어야 한다
#    아니면: pip uninstall -y opencv-python opencv-contrib-python
#           pip install "opencv-contrib-python==4.8.1.78"
ultralytics
