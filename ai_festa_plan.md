# AI Festa 시연 — 구상 정리 (작업용 메모)

작성 2026-09-14.

> ⚠️ **이 문서는 제출용 계획서가 아니다.** "무엇을 어떻게 만들지"를 정하기 위한
> 검토 메모다. 제출용 참가 계획서·제품 소개서는 이 문서를 근거로 **따로** 쓴다.
>
> 이 문서에 적힌 저장소 현황은 2026-09-14 기준 코드·문서 대조 결과다. 판단이 아니라
> **확인된 사실**만 "증거" 칸에 넣었고, 아직 안 정한 것은 §7에 모아 뒀다.

---

## 0. 일정 — 확정된 날짜

| 날짜 | 요일 | 내용 |
|---|---|---|
| **2026-09-18** | 금 | 참가 계획서 + 제품 소개 PDF 마감 |
| **2026-11-13** | 금 | MVP 개발 완료 목표 |
| **2026-11-18 ~ 20** | 수~금 | **현장 시연** (하드웨어 직접 운반) |

오늘부터 MVP 마감까지 **8.5주**. 현장 시연까지는 9주.

**가져갈 장비**: UR16e 팔 + DG-5F-M-R 그리퍼. **AMR은 안 가져간다**(상세 스펙 미확보라
11월까지 위험이 너무 크다).

---

## 1. 시연 시나리오 구상

목표로 잡은 동작:

```text
① 팔이 스스로 움직여 물체를 잡을 수 있는 위치까지 접근   ← 강화학습
② 시연자가 손을 들어 파지 자세 → MediaPipe → 그리퍼가 잡음  ← 사람이 조작
③ 물체가 잡힌 것을 감지                                    ← 모터 전류
④ 알아서 들어올림                                          ← 스크립트
```

**자율 파지(어디를 잡을지 스스로 정하기)는 이번 범위에서 뺀다.** 프로젝트 최종 목적
(범용 자율 파지)의 핵심 난제이고, 저장소 문서가 "최대 기술 격차"로 기록해 둔 항목이라
8주에 들어갈 크기가 아니다.

**②에 사람이 들어가는 것은 결함이 아니라 기획이다.** 시연에서 "사람의 손동작을 로봇
손이 그대로 따라 한다"는 장면 자체가 볼거리가 되고, 이 경로는 **이미 실물에서 검증된
유일한 경로**다.

---

## 2. 4조각 각각의 현재 상태

| # | 조각 | 상태 | 증거 |
|---|---|---|---|
| ① | 팔 자율 접근 | 🟡 **재학습 필요** — 학습된 정책은 있으나 물체 위치를 시뮬레이터에서 공짜로 받는다 | `unity/Assets/MLAgents/picknplace/` · `DG5FPicknPlace.onnx` |
| ② | MediaPipe → 그리퍼 | 🟢 **실물 검증 완료 (2026-08-31)** | `vision/dg5f/dg5f_sdk_bridge.py` · 로드맵 §5 |
| ③ | 파지 감지 | 🟡 **읽는 경로는 있고, 기준값만 미측정** | `dg5f_sdk_bridge.py:324,364` (모터 전류 수신) |
| ④ | 자동 들어올리기 | 🟢 **쉬움** — 팔을 위로 10 cm. 학습 말고 스크립트 | `arm/ur_rtde_bridge.py` |

### ①의 정확한 현황

현행 Unity 정책 `DG5FPicknPlace`:

| 항목 | 값 | 출처 |
|---|---|---|
| 관찰 / 행동 | **57 / 7** (팔 6축 + 손 닫힘 1축) | `Dg5fPicknPlaceSpec.cs:27-28` |
| 대상 물체 | 0.035 × 0.12 m 각기둥 1종 | 같은 파일 `CubeWidth`/`CubeHeight` |
| 물체 놓이는 범위 | 로봇에서 0.37 ~ 0.58 m | `MinimumSpawnRadius`/`MaximumSpawnRadius` |
| 에피소드 제한 | 20초 | `EpisodeTimeoutSeconds` |
| 하드웨어 | **UR16e + 오른손 = 실물과 같음** | `training/README.md` |

**문제는 관찰 13~21번 칸이다.** 물체 상태를 이렇게 계산한다
(`Dg5fPicknPlaceAgent.cs:552`):

```csharp
robotBase.InverseTransformDirection(GraspTargetPosition() - graspPoint.position)
```

즉 **시뮬레이터가 물체 위치를 직접 알려준다.** 카메라를 거치지 않는다. 실물에서는 이
칸을 카메라가 채워야 한다 — 그게 §3의 전부다.

### 참고할 코드 — 팔 단독 접근 behavior가 git 이력에 있다

`DG5FGraspReadyReach` — **팔 6축만으로 파지 가능 위치까지 접근**하는 학습 설정이
과거에 있었다. 관찰 37 / 행동 6 (`training/README.md:10`).

> ⚠️ **그대로 쓸 수는 없다 — 이건 `UR5e` 팔 기준이다.** 현재 확정 하드웨어는
> **UR16e**라 관절 한계·토크·링크 길이가 전부 다르다. **설계 참고용**으로 읽고,
> 실제로는 현행 `DG5FPicknPlace`(UR16e+오른손)에서 파지·들기 부분을 떼어내
> "접근까지만" 남기는 쪽이 맞을 가능성이 높다.
>
> 무엇을 참고할 값어치가 있나: **보상을 어떻게 걸었는가**(접근만 학습시킬 때의
> 성공 판정·셰이핑)와 커리큘럼 구성. 이건 하드웨어가 바뀌어도 그대로 쓸 수 있다.

**꺼내 보는 명령** — 파일마다 삭제된 커밋이 다르다:

```bash
# 학습 설정(하이퍼파라미터·보상) — bdaf3e5 에서 삭제됨
git show bdaf3e5^:training/config/dg5f_grasp_point_reach.yaml
git show 015821b^:training/config/dg5f_grasp_point_reach_v1.yaml   # 더 이전 판

# 학습·평가 스크립트, 커리큘럼 — 5552303 에서 삭제됨
git show 5552303 --stat
git show 5552303^:training/scripts/train_dg5f_grasp_point_reach.sh
git show 5552303^:training/scripts/evaluate_dg5f_grasp_point_reach.py
git show 5552303^:training/policy/ready_reach/ReadyReachCurriculum.cs
```

---

## 3. 카메라가 본 것을 팔에게 넘기는 원리

> **가장 헷갈렸던 부분. 여기만 이해하면 나머지는 배선이다.**

### 3-0. 핵심 — 정책은 영상을 보지 않는다

강화학습 정책이 물체에 대해 받는 것은 **숫자 3개**뿐이다:

> **"잡아야 할 지점이, 지금 손바닥에서 어느 쪽으로 얼마나 떨어져 있는가"**
> — 로봇 기준 x, y, z (미터)

시뮬에서는 시뮬레이터가 알려주고, 실물에서는 카메라가 계산한다. **공급자만 바뀌고
정책은 그대로다.** 이래서 시뮬에서 학습한 것을 실물에 그대로 올릴 수 있다.

### 3-1. 4단계

```text
①  영상에서 물체 찾기        → 화면 픽셀 (u, v)
②  픽셀을 거리로 바꾸기      → 카메라 기준 3D (X, Y, Z) 미터
③  로봇 기준으로 옮기기      → 로봇 기준 3D          ★ 진짜 일은 여기
④  정책에 넣기               → 팔 관절 6개 각도 → RTDE → 팔이 움직임
```

### ① 영상에서 물체 찾기

마커 없이. 선택지 둘:

| 방법 | 내용 | 판단 |
|---|---|---|
| **사전학습 검출기(YOLO 등)** | 컵·병·상자 등 흔한 물체는 추가 학습 없이 잡힌다 | 물체를 자유롭게 바꿔 보일 수 있어 시연 효과가 크다 |
| **배경 빼기** | 빈 탁자를 한 번 찍어두고 달라진 덩어리를 물체로 본다 | 조명·배경이 통제되면 훨씬 안정적. **실패 확률이 낮다** |

> 💭 **고민거리**: 시연장 조명은 통제가 안 된다. 배경 빼기는 조명이 바뀌면 무너지고,
> 검출기는 처음 보는 물체에서 놓칠 수 있다. **둘 다 넣고 현장에서 고르는 것**이 안전할
> 수 있다.

### ② 픽셀 → 미터

```
Z = 그 픽셀까지의 거리(깊이)
X = (u - cx) × Z / fx
Y = (v - cy) × Z / fy
```

`fx, fy, cx, cy`는 **카메라 고유값**이고 체커보드를 여러 각도로 찍어 구한다.
**이 코드는 이미 있다** — `vision/dg5f/calibrate_intrinsics.py`
(보드 규격은 `.env`의 `RTAUTO_CALIB_BOARD_COLS/ROWS`·`RTAUTO_CALIB_SQUARE_SIZE_MM`).

> 💡 **깊이 카메라 없이 가는 길.** 물체가 **항상 같은 높이의 탁자 위**에 있다면 깊이를
> 몰라도 된다 — "카메라에서 그 픽셀 방향으로 뻗은 직선이 탁자 평면과 만나는 점"이
> 3D 위치다. **웹캠 한 대면 된다.** 시연용으로 유력한 후보.

### ③ 카메라 기준 → 로봇 기준 ★

카메라는 "물체가 내 앞 40 cm"라고 말하지만 팔은 **자기 어깨 기준**으로만 움직일 줄
안다. 둘을 잇는 표 하나가 `T_base_camera` — **"카메라가 로봇 어깨에서 어디에 어떤
각도로 놓여 있는가"**(4×4 행렬).

구하는 법: 체커보드를 탁자에 놓고 → 카메라로 찍어 "카메라 기준 보드 위치"를 얻고
→ 팔 끝을 보드 모서리에 직접 대서 "로봇 기준 보드 위치"를 얻고 → 둘을 맞추는 변환 계산.

> ⚠️ **로드맵이 "미구현"으로 경고한 항목이 정확히 이것이다.**
> 리스크 표 원문: *"비전이 물체를 찾아도 팔이 엉뚱한 곳으로 감."*
> (`docs/SIM2REAL_ROADMAP.md` §5-7, §11)
>
> 그리고 **이 부분은 AI가 대신 해줄 수 없다.** 코드는 짧다. 일은 **카메라를 단단히
> 고정하고, 체커보드를 찍고, 팔 끝을 갖다 대서 재는 물리 측정**이다. 카메라가 한 번
> 움직이면 다시 재야 한다.

### ④ 정책 → 팔

**이미 다 있다.** `arm/ur_rtde_bridge.py`가 관절 각도 6개를 UDP 5009로 받아 `servoJ`로
팔에 보낸다. 실제각 되돌림은 5010. URSim으로 검증까지 끝났고 손댈 게 없다.

---

## 4. 카메라를 어디에 달 것인가 — 고정 카메라 쪽으로 기운다

| | 고정 카메라 (eye-to-hand) | 손목 카메라 (eye-in-hand, D405) |
|---|---|---|
| `T_base_camera` | **한 번 재면 끝** | 팔이 움직일 때마다 관절각으로 다시 계산 |
| 오차 | 카메라 오차만 | 카메라 오차 **+ 팔 관절 오차** |
| 접근 시작 시점 | 멀리서도 물체가 보임 | **물체가 시야 밖일 수 있음** |
| 저장소 준비 상태 | 다중 웹캠 캘리브레이션 코드 있음 | 없음 (손목 장착 자체가 보류 상태) |

**시연용으로는 고정 카메라를 권한다.** 손목 카메라는 최종 목적(자율 파지, 파지 직전
정밀 관측)에서 필요한 것이지 접근 단계용이 아니다.

> ⚠️ **확인 필요**: RealSense D405는 **근거리 특화** 카메라로 알려져 있다(최소 측정
> 거리가 짧은 대신 먼 거리가 약함). 접근 시작 지점(로봇에서 0.4~0.6 m)에서 물체가
> 잡히는지 **스펙시트로 확인할 것.** 안 되면 고정 웹캠 쪽이 답이다.

---

## 5. 일정 뼈대 — 비밀번호가 크리티컬 패스다

> 🛑 **UR16e 펜던트의 Automatic(자동) 모드 안전 비밀번호가 없다.**
> 없으면 **PC가 팔을 못 움직인다.** 요청해서 받는 데 며칠~몇 주.
> 근거: `README.md:213`, 로드맵 v9(2026-09-04).
>
> **가장 오래 걸리는 항목이므로 이번 주에 요청을 넣는다.**

**결정적으로 중요한 사실: 실물 팔 없이도 전체를 끝까지 돌려볼 수 있다.**
`카메라 → 물체 위치 → 정책 → URSim(PC 안의 가상 UR16e)` 까지 지금도 된다.
실물이 열리면 **접속 IP만 바꿔 끼운다.** 로드맵이 애초에 이렇게 설계해 뒀다.

| 주차 | 트랙 A — 팔 없이 되는 일 | 트랙 B — 조달·측정 |
|---|---|---|
| 9/14~9/18 | 계획서·소개서 작성 | 🛑 **비밀번호 요청** / 스위치·케이블 주문 |
| 9/22~10/3 | reach 정책 복원 + 랜덤화 넣어 재학습 | 카메라 거치대 제작, 고정 위치 확정 |
| 10/6~10/17 | 물체 검출 + ②③ 좌표 변환 구현, URSim으로 전 구간 연결 | **체커보드 측정 → `T_base_camera` 산출** |
| 10/20~10/31 | 파지 감지 전류 기준값 측정(그리퍼만 있으면 됨), 리프트 스크립트 | 비밀번호 도착 대기 / 스위치 배선 |
| 11/3~11/13 | **실물 팔 개통 → 전이 → 통합 리허설** | 운반·전원·안전 펜스 확인 |
| 11/14~11/17 | 예비 + 현장 설치 | |

**비밀번호가 11월 초에 도착해도 약 2주가 남는다.** 그 전에 나머지를 전부 URSim에서
완성해 두는 것이 이 일정의 성립 조건이다.

---

## 6. 리스크와 보험

| 리스크 | 영향 | 대응 |
|---|---|---|
| **비밀번호 미도착** | 실물 팔 전체가 막힘 | 이번 주 요청. 보험(아래) 준비 |
| **`T_base_camera` 오차** | 팔이 엉뚱한 데로 감 | 10월 중순까지 측정 완료, 남은 기간에 보정 |
| 시연장 조명 변화 | 물체 검출 실패 | 검출 방법 2개 준비, 현장에서 선택 |
| 시뮬→실물 전이 실패 | 팔이 안 움직이거나 헤맴 | **랜덤화를 처음부터 학습에 포함**(현행 정책의 최대 약점이 랜덤화 부재) |
| 팔 속도 설정 미검증 | 실물에서 너무 느리거나 위험 | `UR_MAX_DEG_PER_SEC`는 현재 **근거 없는 추측값 30**. 실물 확보 즉시 측정 |
| **운반 중 파손·현장 전원** | 시연 자체 불가 | 전원·테이블·안전 펜스를 주최측에 미리 확인 |

### 보험 — 지금 당장도 되는 축소 시연

**그리퍼 단독 MediaPipe 시연 + Unity 화면(디지털 트윈).**
그리퍼 경로는 이미 실물 검증이 끝나 있으므로 **실물 팔이 11월까지 안 열려도 시연은
성립한다.** 이것을 "차선책"이 아니라 **처음부터 준비하는 2안**으로 잡는다.

> ⚠️ **주의**: 웹캠 송신과 실물 손 피드백이 **같은 UDP 5006**을 쓴다. 동시에 켜면
> Unity가 받는 값이 섞인다(`vision/README.md`). 시연 구성에서 반드시 갈라놓을 것.

---

## 7. 결정 사항 (2026-09-14)

| # | 항목 | 결정 |
|---|---|---|
| 1 | 학습 엔진 | **Unity로 간다.** SuperDex 작업(`place` 상수 제거 등)은 시연 후 재개 |
| 2 | 소개 PDF의 주인공 | **이번 시연**. 최종 목적은 "다음 단계"로 뒤에 배치 |
| 3 | 시연 물체 | 빈 500 mL 페트병 · 종이컵 등 **무르고 가벼운 물체** (→ §8) |
| 4 | 조작 방식 | Unity 화면에서 보며 원격조작 + 멈춤/리셋 버튼 (→ §10) |

**1번(Unity)의 근거**: SuperDex를 택한 주된 이유가 **접촉 물리 표현력**인데, 이번엔
**파지를 사람이 한다** → Unity의 약점이 안 물린다. 게다가 UR16e+오른손으로 학습된
정책이 **이미 Unity에 있다.**

> ⚠️ 이건 **최종 목적(범용 자율 파지)에서 잠시 벗어나는 것**이다. 시연 후 돌아올
> 지점을 `docs/SUPERDEX_POC_PLAN.md` §0 인수인계 블록에 남겨 둘 것.

---

## 8. 시연 물체 — 종이컵·페트병 (비강체)

### 결론부터: 이 구성에서는 "강체가 아닌 것"이 난이도를 거의 안 올린다

일반적으로 물렁한 물체는 시뮬레이션이 어렵다. 그런데 **이번 시연에서는 그 어려움이
시뮬레이션에 존재하지 않는다:**

| | 어디서 일어나나 | 시뮬이 알아야 하나 |
|---|---|---|
| 팔 접근 | 시뮬에서 학습 | **물체의 "위치"만** 알면 됨. 안 찌그러진다 |
| 파지 | **실물에서, 사람이** | 시뮬 개입 없음 |
| 찌그러짐·변형 | **실물에서만** | 시뮬이 표현할 필요 자체가 없음 |

즉 학습 쪽에서는 종이컵을 **"그 자리에 있는 원기둥"** 으로만 다루면 된다.
**변형체 물리를 구현할 일이 없다.**

### 진짜 걸림돌 3개

**① 투명한 페트병은 깊이 카메라가 못 본다** ← 가장 위험

깊이 카메라(스테레오·적외선 방식)는 투명체에서 깊이값이 비거나 튄다. 빈 생수병이
정확히 그 최악의 경우다. 종이컵은 무광 흰색이라 잘 보인다.

대응:
- **라벨이 붙은 페트병**을 쓴다(라벨 구간은 불투명)
- 또는 **§3의 "탁자 평면 가정" 방식**으로 간다 — 깊이가 아예 필요 없다
- 종이컵을 **주력**으로, 페트병을 **도전 과제**로 배치

**② 가벼워서 밀린다**

빈 페트병 ~20 g, 종이컵 ~5 g. 접근 중 손가락이 스치면 굴러간다.

대응:
- 접근 종료 지점을 물체에서 조금 더 떨어뜨린다
- 굴러가도 **카메라가 새 위치를 계속 주므로 정책이 따라간다** — 폐루프의 장점이
  오히려 여기서 드러난다. 시연에서 "물체를 옮겨도 팔이 따라온다"를 보여줄 수 있다

**③ 쥐면 찌그러진다**

대응: 손가락 목표각을 **덜 닫히게 제한**한다. 이미 `JOINT_CLAMP_RIGHT`(채널별 실측
가동범위)와 브리지의 속도 제한이 있으므로, 물체별 **"최대 닫힘" 프리셋**을 얹으면 된다.

> 💡 **이건 약점이 아니라 시연 포인트다.** "손가락 20개를 따로 제어해서 종이컵을
> 안 찌그러뜨리고 잡는다"는 **다지 그리퍼가 2지 그리퍼보다 나은 이유 그 자체**다.
> 계획서·소개서에서 이걸 앞세울 값어치가 있다.

---

## 9. 파지 감지 — 위치센서와 전류만으로 해야 한다

### 확정: 우리 그리퍼에 핑거팁 센서는 없다 (2026-09-14)

**핑거팁 센서는 구매 시 요청하는 옵션이고, 우리는 그 옵션을 선택하지 않았다.**
사용자 확인 + 제조사 자료 대조로 확정.

| 센서 | 우리 DG-5F-M-R |
|---|:--:|
| **절대 엔코더** (관절 위치) | ✅ **기본 내장** |
| **모터 전류** | ✅ **기본 내장** |
| 6축 F/T 센서 (손끝) | ❌ **옵션 — 미구매** |
| 3축 힘 센서 (손끝) | ❌ 옵션 — 미구매 |
| 촉각 센서 (손끝) | ❌ 옵션 — 미구매 |

제조사 표기 원문: *"Optional 6-axis F/T, 3-axis force, and tactile sensors can be
integrated at the fingertips according to application requirements."*

> ⚠️ **SDK에 API가 있는 것에 속으면 안 된다.** 벤더 파이썬 래퍼에는
> `FINGER_FT_SENSOR = 0x05`, `get_fingertip_sensor_data()`,
> `forceTorque[6×5]`·`tactile[18×5]` 구조체, 예제 `sensor_readout.py`까지 전부 있다.
> 이건 **DG-5F 계열 공용 SDK**라서 그런 것이다. 센서를 안 달았으면 값이 안 들어온다.
> **API가 있다 ≠ 센서가 있다.**

### ✅ 9-1. 로드맵 정정 완료 (v19, 2026-09-14)

`docs/SIM2REAL_ROADMAP.md`가 **핑거팁 센서가 실재한다고 단정**하고 그 위에 관찰 설계를
세워 뒀었다. **7곳을 고치고 개정 이력 v19를 추가했다.**

| 위치 | 원래 서술 | 정정 내용 |
|---|---|---|
| §2 절 제목 | *"병목은 **비전 하나**로 좁혀졌다"* | → *"블로커는 **비전과 접촉 둘**이다"* |
| §2 관찰표 | 접촉 플래그 6칸 조달처 = SDK **✅** | → **조달처 없음 ❌** |
| §2 본문 | *"센서가 **실재하므로** 접촉 관찰은 확보된다"* | → 정정 블록. **"접촉 센싱 불가" 분기는 소멸하지 않았다** |
| §2 eye-in-hand | *"접촉 순간부터 핑거팁 FT 센서로 감각이 넘어간다"* | → **넘겨받을 센서가 없다. 이 구간이 설계 최대 공백** |
| §5 착수항목 3 | *"핑거팁 센서 실측 — 임계값 결정"* | → **접촉 추정기 실측**(전류·추종오차)으로 대체 |
| §5 "남은 것" | *"핑거팁 센서 임계값"* | → *"접촉 추정기 임계값"* |
| §5 Modbus 각주 | *"`128~` 존재를 전제"* | → **센서 데이터 영역은 비어 있을 가능성. 기대지 말 것** |
| §5 센서 역할표 | *"그리퍼 촉각 센서 → 접촉 이후"* | → **담당 센서 영구 부재**(구매 기각) |

> ✅ 반면 `docs/RL_POLICY_REDESIGN.md` §1은 **이미 올바르게 유보**해 뒀었다 —
> *"팜 센서 유무를 확인하기 전에는 '실물 접촉 확보 완료'로 간주하지 않는다."*
> 그 판단이 옳았다. **이 문서는 손대지 않았다.**

**남은 진짜 문제는 시연이 아니라 최종 목적 쪽이다.** 범용 자율 파지의 관찰 설계가
"손끝 접촉 6칸"을 전제로 서 있는데 그 입력이 실물에 없다. 전류·추종오차 추정은
**불리언 접촉 6칸과 등가가 아니다**(어느 손가락이 무엇에 닿았는지 모른다). 관찰 계약을
🛑 **센서 추가 구매는 기각됐다**(2026-09-14, 비용). 따라서 **관찰 계약을 다시 쓰는 것이
유일한 길**이고, 이는 시연 후 최우선 설계 작업이 된다.

### 9-2. 그래서 파지 감지는 이렇게 한다 — 위치센서 + 전류

| 방법 | 원리 | 준비 상태 |
|---|---|---|
| **추종 오차** | "이만큼 접어라" 명령했는데 실제각이 안 따라오면 → 뭔가에 막힌 것 = 잡은 것 | `dg5f_sdk_bridge.py --track CSV`가 **이미 명령각 vs 실제각을 기록한다** |
| **전류** | 막혀 있으면 모터 전류가 오른다 | **이미 받고 있다** (`dg5f_sdk_bridge.py:324` `CURRENT`). 실측 사례도 있음 — 0 mA=미구동 / 1.5 A=밀고 있음 |

**조합하면**: *실제각이 명령보다 덜 닫혔다* **+** *전류가 올라갔다* = 잡았다.

⚠️ 종이컵처럼 무른 물체는 저항이 약해 임계값이 낮아진다 → **물체별 프리셋**이 필요하다.

### 그런데 라이브 시연에서는 사람을 신호로 쓰는 게 가장 확실하다

시연자가 **파지 자세를 1~2초 유지** → "잡았다고 선언" → 들어올리기 시작.
센서·전류는 **안전 확인용**으로만 쓴다(전류가 너무 높으면 중단).

관람객 앞에서 임계값이 빗나가 안 들어올리는 것보다 훨씬 안정적이다.
**시연용 판정과 최종 목적용 자동 판정을 분리**해서 생각한다.

---

## 10. Unity 원격조작 구상 — 가능한가?

구상한 것: *Unity Game 화면에 3D 카메라로 본 물체가 실시간으로 생기고, 로봇이
디지털 트윈으로 따라 움직이고, 멈춤 버튼으로 동작을 취소한다.*

**대부분 가능하고, 절반은 이미 만들어져 있다.**

| 원하는 것 | 가능? | 근거 |
|---|---|---|
| 팔 실제 움직임이 Unity에 실시간 반영 | ✅ **이미 됨** | `UrArmReceiver.cs` + `UrArmTwinDriver.cs`, URSim으로 검증 완료 |
| 손 실제 움직임 반영 | ✅ **이미 됨** | `Dg5fReceiver.cs` + `Dg5fHandDriver.cs` |
| 홈 자세 복귀 버튼 | ✅ **이미 있음** | `PicknPlaceArmJointPanel.FullReset()` + 화면의 "초기화 (전체 리셋)" 버튼 |
| 카메라가 본 물체가 Unity에 생성 | 🟡 **새로 만들어야 하나 쉬움** | `Dg5fReceiver.cs`의 UDP 수신 패턴을 그대로 복사. 물체 생성은 `RandomPillarSpawner.cs` 패턴 |
| 멈춤/취소 버튼 | 🟡 **패턴 있음** | Sender들이 **기본 꺼짐**이고, `Dg5fTwinModeSwitcher.cs`가 이미 버튼으로 실물 모드를 바꾼다 |

> 참고: 예전에 이 역할을 하던 `CameraTargetReceiver.cs`가 있었으나 ZED 경로 폐기와
> 함께 삭제됐다(`vision/README.md` §4). 새로 만든다.

### 🛑 반드시 짚어야 할 것 — Unity 버튼을 비상정지로 쓰면 안 된다

- UDP는 패킷이 사라질 수 있다.
- 그리고 **브리지는 UDP가 끊기면 "멈추는" 게 아니라 마지막 자세를 유지한다**
  (`docs/modules/REAL_BRIDGES.md` §4에 명시).
- 로드맵도 못박아 뒀다 — *"비상정지는 예외 없이 유선."*

**따라서 둘을 분리한다:**

| | 무엇 | 누가 |
|---|---|---|
| **동작 취소** | Unity 화면의 버튼 (다음 명령 중단 + 홈 복귀) | 조작자 |
| **비상정지** | **UR 펜던트의 물리 빨간 버튼** | 시연 중 **한 사람이 펜던트를 들고 있어야 한다** |

> ⚠️ `FullReset()` 주석이 이미 경고한다 — **"초기화 전에 송신을 끄는 쪽이 안전하다."**
> 리셋 버튼은 "송신 끄기 → 홈 복귀 → 송신 켜기" 순서로 묶어야 한다.

### 또 하나 — Unity의 물체는 "보여주기용"이다

카메라가 준 위치에 원기둥을 띄우는 것은 **시각화**이지 물리 계산이 아니다.
`CLAUDE.md`가 *"Unity 트윈으로 파지 물리를 검증하지 않는다"* 고 못박아 뒀는데,
**시각화는 그 금지에 걸리지 않는다.** 다만 화면의 종이컵이 찌그러지는 모습은
안 나온다는 것을 시연 설명에 반영할 것.

---

## 10-1. 아직 안 정한 것

1. **물체를 몇 종 보일 것인가** — 1종 고정이 안전하지만 밋밋하다. 학습 시 물체
   크기를 랜덤화하면 여러 종이 가능하지만 검증 부담이 는다.
2. **시연 1회 길이** — 접근 몇 초, 파지 몇 초, 전체 몇 분. 관람객 회전율과 직결된다.
3. **조작자가 몇 명 필요한가** — 최소 2명으로 보인다(MediaPipe 조작 1 + 펜던트 1).
   1명으로 줄일 수 있는지 검토.
4. ~~**핑거팁 센서 유무**~~ → **2026-09-14 확정: 없다**(§9). 대신 정해야 할 것은
   **전류·추종오차 임계값을 물체마다 어떻게 정할 것인가**이고, 그리퍼를 PC에 붙일 수
   있게 된 뒤 `--track`으로 실측해야 한다.
5. ~~**로드맵 정정**~~ → 완료(v19). ~~**센서 추가 구매 여부**~~ → **기각**(2026-09-14, 비용).
   대신 남은 진짜 숙제: **"접촉이 추정 가능한 물체"를 작업 범위에 어떻게 정의할 것인가.**
   추종오차는 손가락이 막혀야 생기는데, **종이컵·빈 페트병은 막히기 전에 먼저 찌그러진다.**
   즉 이번 시연 물체가 바로 **추정이 가장 안 되는 부류**다(시연은 사람이 잡으니 무관하지만,
   최종 목적에서는 정면 문제). `RL_POLICY_REDESIGN.md` §0-2의 작업 범위에
   크기·질량·개구폭·관측 품질에 더해 **강성 조건**을 추가해야 한다.

---

## 11. 계획서 쓸 때 주의

> ⚠️ **시뮬레이션 성공률을 성능 지표로 쓰지 말 것.**
>
> `picknplace_gl_20260830`의 99.21 %는 **12 cm 각기둥 1종, 랜덤화 없음** 조건의
> 값이고 실물 배포용이 아니다(물체 무게중심만 바꿔도 99.7 → 96.6 %로 떨어진다).
> 로드맵 리스크 표가 이미 경고해 뒀다 — *"보고서에 성공률을 KPI로 걸면 실패로
> 보고됨."*
>
> 계획서의 성공 기준은 **"현장에서 N회 중 M회 시연 성공"** 처럼 시연 자체로 잡는다.

---

## 참고 문서

| 문서 | 무엇의 정본인가 |
|---|---|
| `docs/SIM2REAL_ROADMAP.md` | 아키텍처·일정·리스크 **최상위 정본**. §5가 실물 연결 경로 |
| `docs/modules/REAL_BRIDGES.md` | 팔·손 브리지 인자·패킷·SDK 호출 |
| `docs/modules/VISION_TELEOP.md` | MediaPipe 텔레옵 파이프라인 |
| `docs/RL_POLICY_REDESIGN.md` | 관찰·행동·보상 계약. §0-2가 최종 목적의 작업 범위 |
| `docs/SUPERDEX_POC_PLAN.md` | 파지 RL 현황. §0에 인수인계 블록 |
| `docs/TRAINING_RUN_LEDGER.md` | 학습 런별 실측 수치. §11의 99.21 % 출처(`picknplace_gl_20260830` 행) |
| `training/README.md` | Unity ML-Agents 학습·평가 명령 |
| `config/rtauto_config.py` | 모든 IP·포트·경로의 유일한 출처 |
