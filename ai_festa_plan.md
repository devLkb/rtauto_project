# AI Festa — 디지털 트윈 기반 사람–로봇 협업 작업 시연

작성·개정: 2026-09-14.

> 이 문서는 제출용 참가 계획서·제품 소개서의 근거가 되는 작업 계획이다.
> **기본 기능 범위는 §1~§5를 따른다. §6은 일정·검증 기준 제안, §7은 미결정 사항,
> §8은 추가 계획, §9는 문서 재검토에서 확인한 미해결 항목이다.**
> 기본 기능 범위의 합의가 구현 완료나 세부 실행 조건·KPI의 확정을 의미하지 않는다.
> 추가 계획을 기본 MVP의 필수 기능이나 제출용 성능 약속으로 기재하지 않는다.
> 이번 합의는 기존 메모의 RL 접근·자동 파지 판정·고정 카메라 구상을 대체한다.
> 아래 기능은 개발 목표이며, 기존 경로가 있다는 이유만으로 실물 통합 완료로 간주하지 않는다.

> ## 📌 2026-09-17 확인 — 제출한 약속과 현장에서 보여줄 것이 다르다
>
> | | 범위 |
> |---|---|
> | **제출한 참가 계획서** | **웹캠에 손을 비추면 그리퍼가 따라 움직이는 것까지.** 팔은 약속하지 않았다 |
> | **현장에서 실제로 할 것** | 팔도 가져가서 움직인다(사용자 확인) |
>
> 즉 **대외 약속이 개발 목표보다 좁다.** 이건 안전한 방향이다 — 팔이 끝내 안 되더라도
> 약속을 어기는 것이 아니다. 아래 §3 전체 사이클은 **개발 목표**로 읽고, **대외 발표·
> 홍보 문구에는 제출 범위를 넘는 약속을 쓰지 않는다.**
>
> §8 자동 접근(카메라로 보고 팔이 스스로 다가가기)은 **제출 범위에도 현장 필수 범위에도
> 들어 있지 않다.** 별도 개발이며 정본은
> [`docs/FESTA_PREGRASP_PLAN.md`](docs/FESTA_PREGRASP_PLAN.md)다.

## 1. 확정 소개 문구

> **디지털 트윈 기반 사람–로봇 협업 작업 시연**
>
> 웹캠으로 인식한 사용자의 손동작을 다지 로봇 손의 제어 명령으로 변환하여 실제
> 물체를 파지한다. 로봇 팔은 사전에 정의된 작업 경로를 따라 접근·들기·내려놓기를
> 수행하며, 사용자가 주요 단계의 실행을 확인한다. 실제 로봇의 관절 상태는
> Unity 기반 가상 공간에 반영된다. 이를 통해 사람의 직관적인 조작과 실물 로봇의
> 작업, 가상 공간의 상태 모니터링이 연결되는 과정을 시연한다.

## 2. 장비와 범위

**카메라 보유 현황은 사용자 확인(2026-09-14)을 기준으로 한다. 웹캠 3대 계획은
취소됐으며, MediaPipe용 웹캠 1대와 eye-in-hand용 D405 1대만 있다. 추가 장비 구매는 불가하다.**

| 장비 | 역할과 범위 |
|---|---|
| UR16e | 사전에 정의하고 실물에서 검증한 접근·들기·내려놓기·복귀 경로 수행 |
| DG-5F-M-R 오른손 | 사람 손동작에 따른 파지, 실제 관절 상태 피드백 |
| 웹캠 1대 | 조작자 손 촬영 → MediaPipe 손동작 인식 |
| RealSense D405 1대 | 보유 장비. 영상 표시·자동 접근은 §8 추가 계획이며 기본 시연에는 사용하지 않음 |
| 기존 시연 PC | Python 장치 연결·제어와 Unity 조작 화면 실행 |
| 엔스퀘어 AMR | **전시장에 가져가되 주행하지 않는다.** 구성품 전시용 |

- **AMR은 가져간다. 다만 주행은 하지 않는다** (2026-09-18 변경 — 이전에는 "가져가지 않는다" 였다). 전시장에 놓고 **구성품으로 보여 주기만** 한다.
  자율주행·Nav2·이동 중 파지는 **이번 전시 범위 밖**이다. 매니퓰레이션은 CLAUDE.md "동작 원칙" 대로 **베이스가 정지한 상태**에서만 일어난다.
- D405 보유와 손목 장착·영상 연동 완료는 별개다. 관련 검증은 추가 계획 착수 시 진행한다.
- 케이블·네트워크·장착 부품도 기존 보유품으로 구성한다. 사용 가능 수량과 설치
  가능 여부를 확인하며, 신규 주문을 일정의 전제로 두지 않는다.
- 손끝 힘·촉각 센서는 미장착이다. 관절 위치와 모터 전류를 읽을 수 있다는 사실을
  파지 성공이나 접촉력 측정이 확보됐다는 의미로 해석하지 않는다.
- 범용 자율 파지는 장기 목표로 유지하되, 이번 시연은 사람 협업과 상태 모니터링에 집중한다.

## 3. 기본 시연 한 사이클

물체는 지정 위치에 놓는다. 첫 검증은 같은 규격의 물체 1종으로 진행하며,
종이컵·빈 페트병 등 후보 중 실제 손으로 반복 파지가 가능한 대상을 선정한다.

| 단계 | 사용자 역할 | 로봇·화면 동작 |
|---|---|---|
| 준비 | 물체 배치, 연결·자세 확인 후 시작 | 실제 팔·손 관절 상태와 연결 상태 표시 |
| 접근 | 동작 확인 | 정의된 경로로 파지 준비 위치까지 이동 후 정지 |
| 파지 | 웹캠 앞에서 손동작으로 그리퍼 조작 | 팔은 정지하고 손은 사용자 입력을 따름 |
| 들기 | 파지 결과 확인 후 실행 버튼 누름 | 파지 목표를 유지하며 정의된 높이로 이동 |
| 내려놓기 | 실행 확인 | 지정 위치로 이동 후 정지 |
| 놓기·복귀 | 손 조작으로 물체를 놓고 복귀 실행 | 검증한 경로로 대기 위치 복귀 |

### 손 제어권과 사용자 확인

- 손동작은 손가락 제어에 사용한다. 손동작으로 팔 전체를 자유롭게 조종하는 기능은 범위 밖이다.
- 들기·이동 중에는 파지 목표를 유지하여 웹캠에서 손을 빼거나 손 자세가 바뀌어도
  즉시 놓기 명령으로 이어지지 않도록 한다. 이는 구현·검증할 기능이며 파지 보장은 아니다.
- 실시간 손 조작으로 돌아올 때 목표 자세가 급변하지 않도록 전환을 검증한다.
- 들기는 사용자의 명시적인 확인으로 실행한다. 손 자세 1~2초 유지나 전류 임계값만으로
  자동 시작하지 않는다.
- 물체가 밀리거나 파지가 실패하면 작업을 취소하고 재배치한다. 자동 추종·재파지는 하지 않는다.

## 4. 손동작 입력과 디지털 트윈의 정확한 의미

### 기본 시연 입력

| 입력 | 처리 | Unity에서의 용도 |
|---|---|---|
| 웹캠 영상 | MediaPipe로 사용자 손동작 추출 | 손 조작 입력, 필요시 손 추적 미리보기 |
| 실물 관절 피드백 | 실제 팔·손 관절 상태 수신 | 3D 로봇 자세 표시 |

**D405 영상 표시는 기본 시연에서 제외한다.** 영상 표시는 §8-1, 물체 인식·자동 접근은
§8-2 이후의 추가 계획으로만 둔다. 사용자는 실물을 직접 보며 파지·작업 결과를 확인하고,
Unity에서는 실제 로봇의 관절 상태를 확인한다. 별도의 작업 카메라 구매는 없다.

### Unity 표시와 제어

- 주 3D 로봇은 **명령 목표가 아니라 실물에서 받은 관절 상태**를 반영한다.
  물체 때문에 실물 손이 덜 닫히면 화면도 실제 자세를 보여주는 것이 목표다.
- 작업 단계, 팔·손 연결, 손 추적 상태와 데이터 갱신 상태를 표시한다.
  오래된 데이터가 정상 실시간 상태처럼 보이지 않도록 한다.
- 작업 버튼은 시작·들기·내려놓기·동작 취소·복귀를 구분한다.
- 동작 취소 후 자동으로 홈 복귀시키지 않는다. 복귀는 주변 상태 확인 후 별도 실행한다.
- Unity 버튼은 물리 비상정지를 대체하지 않는다. 펜던트 비상정지와 현장 운영 절차를 유지한다.
- 물체의 변형·접촉력·자동 파지 판정은 재현하지 않는다. 실제 물체 추적이 없는 상태에서
  가상 물체의 이동을 실제 관측 결과처럼 표현하지 않는다.

## 5. 구현 경계와 현재 출발점

**이번 시연을 위해 ROS2를 도입하지 않는다.** 기존 Python·Unity·RTDE 경로를 활용한다.

```text
웹캠 → 기존 MediaPipe Python → 손 조작 명령 → DG-5F
Unity 작업 버튼 → 작업 단계·경로 제어 → 기존 RTDE 경로 → UR16e
DG-5F / UR16e 실제 관절 상태 → Unity 3D 로봇
```

| 항목 | 기존 근거와 남은 작업 |
|---|---|
| MediaPipe → 실물 손 | README에 실물 검증 기록 있음. 시연 물체·운영 조건에서 재검증 |
| 팔 명령·피드백 | `arm/ur_rtde_bridge.py`, `UrArmSender`/`UrArmReceiver`/`UrArmTwinDriver` 경로 있음. URSim 검증과 실물 검증을 구분 |
| 실제 손 피드백 | SDK 읽기·echo 경로 있음. 웹캠 명령과 실제 상태를 분리해 동시 사용 검증 |
| 전체 작업 사이클 | 접근·손 조작·목표 유지·들기·내려놓기·복귀와 전환 제어 구현 필요 |
| 취소·복구 | 기존 UI 리셋을 실물 복귀로 간주하지 않음. 통신 중단·재시작·제어권 전환 검증 필요 |

**기존 충돌:** 웹캠 입력과 실물 손 echo가 같은 UDP 5006을 쓰는 구성이 문서에 기록돼 있다.
명령과 실제 피드백을 분리해야 한다. 포트·IP·경로 설정은 기존
`config/rtauto_config.py` 체계를 따른다.

## 6. 일정과 검증

### 확정 일정

| 날짜 | 내용 |
|---|---|
| 2026-09-18 | 참가 계획서·제품 소개 PDF 마감 — ✅ **제출 완료**(2026-09-17, 사용자 확인) |
| 2026-11-13 | MVP 개발 완료 목표 |
| 2026-11-18~20 | 현장 시연 |

### 개발 순서 제안

| 기간 | 목표 |
|---|---|
| 9월 중·말 | 제출 문구 확정, 보유 부품 확인, 실물 팔 사용 제약 해소 추진, 손 명령·피드백 분리 |
| 10월 전반 | 실제 상태 표시, 실물 팔 경로 검증, 첫 전체 작업 사이클 연결 |
| 10월 후반 | 반복 파지, 손 제어권 전환, 실패·통신 중단·재시작 복구 검증 |
| 11월 1~13일 | 기능 확장 중단, 운반 후 재설치·시야 확인·통합 리허설 |
| 11월 14~17일 | 예비 기간·현장 설치 준비 |

실물 UR16e의 자동 모드 비밀번호 문제는 기존 문서상 미해결이며 해소 여부를 확인해야 한다.
URSim으로 통신·작업 흐름 개발은 선행할 수 있으나 **실물 검증을 IP 변경만으로 대체할 수 없다.**
11월에 처음 실물 팔을 구동해도 충분하다는 기존 일정 전제는 폐기한다.

### 기본 시연 완료 기준

- 준비부터 파지·들기·내려놓기·복귀까지 실제 장비로 한 사이클을 수행한다.
- 실제 팔·손 상태가 화면에 반영되고, 수신 중단이 명확히 표시된다.
- 손 조작↔파지 목표 유지 전환에서 갑작스러운 목표 변화가 없는지 확인한다.
- 파지 실패·손 추적 손실·통신 중단 후 취소와 재시작 절차를 검증한다.
- 반복 성공 횟수와 실패 후 복구 시간을 기록한다. 예: 20회 중 18회 완료·1분 내 복구는
  **합격 기준 후보**이며 실제 성능이나 확정 KPI가 아니다. 최종 기준은 리허설 전에 정한다.
- 시뮬레이션 학습 성공률을 현장 성능으로 제시하지 않는다.

실물 팔이 끝내 사용 불가능하면 손 단독 MediaPipe·Unity 시연을 별도 축소안으로 준비한다.
이 경우 팔을 포함한 기본 MVP를 완료했다고 보고하지 않는다.

## 7. 남은 결정과 제약

- 실제 반복 파지로 물체 종류·규격·배치·허용 닫힘 범위를 정한다. 부드러운 물체의
  찌그러짐과 미끄러짐은 사람 조작만으로 사라지는 문제가 아니다.
- 팔 실물 사용 가능일, 보유 케이블·네트워크 부품과 현장 전원·작업대 조건을 확인한다.
- 시연 1회 길이와 현장 운영 인원을 정한다. 1인 개발이라는 제약을 1인 현장 운영 확정으로 해석하지 않는다.
- 작업 범위·속도·정지·복귀 경로는 실물에서 검증한다.
- 추가 장비 구매, ROS2 도입, RL 재학습은 기본 MVP의 전제가 아니다.

## 8. 추가 계획 — D405 영상 표시 및 비전 기반 자동 접근

> **추가 검토 사항이며 제출용 기본 계획과 MVP 완료 조건에 포함하지 않는다.**
> 기본 시연이 안정화되고 개발·실물 검증 여력이 있을 때 별도로 착수 여부를 판단한다.
> 구현 일정·성능은 약속하지 않는다. 추가 구매 없이 웹캠 1대+D405 1대를 유지한다.

### 8-1. D405 근접 영상 표시

**미검증 기능이므로 확정 소개 문구·기본 개발 일정·MVP 완료 기준에서 제외한다.**
기본 시연을 먼저 완성하고, 여력이 있을 때 작업 대상의 근접 관찰을 돕는 기능으로 검토한다.
영상 표시와 다음 절의 물체 인식·자동 접근은 각각 별도 추가 기능이다.

```text
D405 → USB → Python RealSense SDK → 영상 전달 → Unity 근접 영상 패널
```

- ROS2 없이 Python 인터페이스로 영상을 취득하는 방침이다. SDK 설치와 Python→Unity
  영상 전달·표시는 구현 및 검증이 필요하다.
- 기존 보유 부품으로 손목 장착이 가능한지, 접근·파지·들기·내려놓기 단계별로 무엇이
  보이는지와 영상 지연을 확인한다. 손가락·물체에 의한 가림을 기록한다.
- 영상 전송은 기존 관절 패킷과 분리한다. 영상 표시만을 위해 물체 검출·깊이 기반
  위치 추정·카메라→로봇 좌표 변환을 필수로 넣지 않는다.
- 영상만으로 모든 작업 결과를 판단할 수 있다고 약속하지 않는다.
- 영상 표시를 구현하지 못하더라도 기본 MVP 완료 여부에는 영향을 주지 않는다.

### 8-2. 물체 인식·자동 접근 — 목표와 제한

D405로 제한된 작업 구역 내 물체 위치를 측정하고, 사람이 잡을 수 있는 준비 위치까지
팔이 자동 접근한 뒤 정지한다. 이후 파지는 기본 시연처럼 사람이 담당한다.

- 시작은 물체 1종·같은 규격·세워 놓은 상태·제한된 배치 구역으로 고정한다.
- 접근 방향과 열린 손 자세, 물체 기준 파지 준비 오프셋을 사전에 정의한다.
- 첫 단계는 관측 자세 1곳에서 **정지 관측 → 목표 확정 → 접근**이다.
- 목표 확정 후 물체를 움직이지 않는다. 위치가 불확실하면 접근하지 않고 재관측한다.
- 안정화 후 미리 정한 관측 자세 2~3곳을 순회하는 제한적 탐색을 검토한다.
- 자유 공간 탐색, 임의 장애물 회피, 범용 파지 자세 생성, 움직이는 물체 추종은 제외한다.

### 8-3. 자동 접근의 ROS2 없는 연결 방침

```text
D405 → USB → Python(pyrealsense2)
  → 영상·깊이 취득 → 물체 검출·유효 깊이 선택 → 카메라 기준 3D 위치
  → 실제 팔 자세와 손목 카메라 보정을 적용 → 로봇 기준 목표
  → 접근 자세·경로 계산 → 기존 RTDE 경로 → UR16e
```

ROS2는 필수가 아니다. SDK는 영상·깊이 접근을 제공하지만 물체 인식이나 팔 경로를
자동으로 완성해 주지는 않는다. Python에서 처리하고 Unity와 기존 통신 구조로 연결한다.
영상 패널과 목표 좌표 전달은 별도 기능으로 검증한다.

손목 카메라 위치 변환의 개념은 다음과 같다.

```text
T_base_camera = T_base_flange × T_flange_camera
```

`T_A_B`는 B 좌표를 A 좌표로 바꾸는 변환이다. 실제 플랜지 자세와 고정된 카메라 장착
변환을 사용하며, TCP 자세를 사용할 경우 TCP↔플랜지 오프셋을 구분해야 한다.
움직이며 관측하는 확장에서는 영상 촬영 시각과 실제 팔 자세의 대응도 검증한다.

### 8-4. 자동 접근 구현 순서와 검증 관문

1. D405 영상·깊이 취득과 실제 장착 시야를 확인한다.
2. 중간 검증에서는 영상의 목표점을 사람이 클릭하여 위치 측정·좌표 변환부터 확인한다.
   클릭은 최종 자동 인식 시연을 대신하는 기능이 아니다.
3. 로봇 기준 측정 위치를 실물 기준점과 대조하고 접근 가능한 오차 범위를 정한다.
4. 물체를 자동 검출하도록 교체한다. 깊이의 결측·이상치와 검출 손실을 처리한다.
5. 실제 손 크기·방향·테이블 여유를 반영한 파지 준비 목표와 경로를 검증한다.
6. 배치 위치를 바꾸며 접근 완료 후 사람이 실제로 잡을 수 있는지 반복 확인한다.

강화학습은 필수 조건이 아니다. 우선 목표 위치와 정해진 손 자세에서 관절·경로를
계산하는 방식을 검토한다. 기존 `DG5FPicknPlace`는 접촉·파지 진행 등 시뮬레이터 입력도
사용하므로 카메라 좌표 3개만 넣어 그대로 실물 배포할 수 있다고 가정하지 않는다.

핵심 불확실성은 D405의 실제 시야·유효 깊이, 손목 장착 보정 오차, 접근 자세와 실물
경로 검증이다. 투명·변형 물체의 깊이 품질과 파지 가능성은 별도로 확인한다.

공식 참고: [RealSense SDK 및 D405·Python 지원](https://github.com/realsenseai/librealsense),
[Python 영상·깊이 정렬 예제](https://github.com/realsenseai/librealsense/blob/master/wrappers/python/examples/align-depth2color.py).

## 9. 문서 재검토 — 실행 전 명확히 할 미해결 항목

기록: 2026-09-14. 문서와 관련 코드의 읽기 검토 결과이며 실물 시험 결과가 아니다.
**확정 소개 문구와 기본 기능 범위는 유지한다. 아래는 새 기능 요구가 아니라 기존 목표를
구현·검증하기 위해 남아 있는 공백이다.** 해결 방식·수치·기한은 아직 확정하지 않았다.

우선순위는 기본 시연 성립에 미치는 영향 순이다. 확인된 문서·코드 공백의 확신도는 높지만,
실제 개발 기간과 실물 성능은 미확정이다. 이 기록을 해결 완료나 구현 승인으로 해석하지 않는다.

### 9-1. 명령 송신과 실제 상태 표시의 동시 수행

- **근거:** §4는 실제 관절 상태를 주 3D 모델에 반영한다고 명시한다. 현재
  `unity/Assets/Scripts/UrArmTwinDriver.cs`의 되먹임 경고와
  `unity/Assets/MLAgents/picknplace/Runtime/PicknPlaceArmJointPanel.cs`의 `SetDirection()`은
  팔 명령 송신과 실제 자세 반영을 같은 모델에서 동시에 사용하는 제약을 보여준다.
  UI의 두 방향 버튼은 상호 배타적으로 동작한다.
- **판단:** §5의 UDP 포트 분리만으로 동시 조작·실제 상태 표시가 완성되지는 않는다.
  명령 목표를 만드는 데이터와 화면에 표시할 실제 상태를 구분하는 작업이 필요하다.
- **미정:** 명령 데이터의 소유 컴포넌트, 실제 상태 표시 경로, 송신 중 피드백이 명령 목표를
  덮어쓰지 않도록 할 구조. 구현 방식을 이 기록에서 특정하지 않는다.
- **해결 확인:** 로봇을 조작하는 동안 실제각이 계속 표시되고, 명령각과 실제각이 다를 때도
  표시가 명령으로 대체되거나 피드백이 명령을 덮어쓰지 않는지 검증한다.

### 9-2. 파지 목표 유지의 정의와 손 피드백 갱신

- **근거:** §3은 들기·이동 중 목표 유지와 실시간 손 조작 복귀를 요구하지만 유지할 값은
  지정하지 않는다. `vision/dg5f/dg5f_sdk_bridge.py`는 명령 처리 뒤에 실제각을 읽고 echo하며,
  수신 타임아웃에서는 읽기 단계 전에 `continue`한다.
  [브리지 입력·출력 설명](docs/modules/REAL_BRIDGES.md)에도 이 제한이 기록돼 있다.
- **판단:** 마지막 사용자 목표각, 브리지에서 제한한 명령각, 실제 관절각은 서로 다를 수 있다.
  어느 값을 유지하느냐가 파지 동작에 영향을 준다. 목표 유지를 송신 중단으로 구현할 경우
  현재 손 피드백 갱신도 멈출 수 있다. 신호 손실로 마지막 값이 남는 것과 의도적 고정은 다르다.
- **미정:** 저장할 값, 고정 시작·해제 조건, 유지 중 명령 송신 및 상태 읽기 방식,
  손 추적 손실과 의도적 고정의 표시 구분, 실시간 조작으로 돌아오는 절차.
- **해결 확인:** 유지·들기 중 실제 상태가 갱신되는지, 추적 손실과 고정이 구별되는지,
  조작 복귀 때 목표가 급변하지 않는지 확인한다. 목표 유지가 파지 성공을 보장하지는 않는다.

### 9-3. 사전 정의 경로와 단계 완료 조건

- **근거:** §3에는 지정 경로와 위치가 있지만 경로 작성·저장·실행 방법은 없다.
  [팔 브리지 역할](arm/README.md)은 관절 명령 전달이며 경로 계획이 아님을 명시한다.
- **미정:** 펜던트 교시 또는 저장된 관절 자세 등 경로 준비 방식, 실행 주체,
  시작 자세 확인, 목표 도착·정지 판정, 도달 실패 시 처리.
- **미정:** 내려놓기가 원래 위치로 돌아오는 것인지 다른 위치로 옮기는 것인지,
  물체 배치 허용 범위와 들기 높이·유지 시간.
- **판단:** 비전·RL을 제외해도 경로 준비·단계 제어 작업은 남는다.
  명령 전송 완료와 실제 목표 도착은 구분해야 한다.
- **해결 확인:** 경로와 배치 조건이 재현 가능하게 기록되고, 실제 도착·정지 확인 후
  다음 단계로 넘어가는지 검증한다.

### 9-4. 취소·실패·재연결 시 팔과 손의 처리

- **근거:** §4는 취소와 복귀를 분리하지만 물체를 들고 있는 도중 취소했을 때의
  팔·손 동작은 정하지 않는다. [정지·연결 해제 설명](docs/modules/REAL_BRIDGES.md)에 따르면
  현재 UDP 중단 처리는 새 목표 전송을 멈추고 안내하는 것으로, 작업 전체의 취소 정책과 다르다.
- **미정:** 접근·파지·들기·이동 각 단계에서 취소할 때 팔과 손을 각각 어떻게 처리할지,
  통신·손 추적 복구 뒤 자동 재개를 허용할지, 어떤 확인을 거쳐 복귀·재시작할지.
- **판단:** ‘취소 후 재배치’만으로 물체를 들고 있거나 이동 중인 실패를 설명할 수 없다.
  물리 비상정지와 소프트웨어 취소의 구분도 계속 유지해야 한다.
- **해결 확인:** 단계별 실패 시나리오에 팔·손 처리와 재개 조건이 정의되고 실물 검증된다.
  단순 통신 재연결 성공을 작업 복구 성공으로 간주하지 않는다.

### 9-5. 합격 기준의 구체성

- **근거:** §6은 화면 반영·급격한 변화 없음·복구 검증을 요구하지만 수치와 시험 조건은
  미정이다. 20회 중 18회·1분 내 복구도 확정 KPI가 아닌 후보다.
- **미정:** 관절 표시 오차·지연 허용치, 오래된 데이터 판정 시간, 들기 높이·유지 시간,
  물체 손상 허용 여부, 한 회 성공의 시작·끝, 재시도 집계, 복구 시간의 시작·끝.
- **판단:** 현재 표현만으로는 일회성 동작과 반복 가능한 전시 시스템을 동일하게
  완료 처리할 여지가 있다. 검증 전 추측한 수치를 실제 성능처럼 쓰지 않는다.
- **해결 확인:** 반복 시험 전에 측정 항목·조건·합격 기준을 정하고 결과를 기록한다.

### 9-6. 실물 지연 시 축소안 전환 시점

- **근거:** §6은 10월 전반 전체 사이클과 손 단독 축소안을 제시하지만,
  ‘끝내 사용 불가능하면’ 외에 전환 결정 시점은 없다. §7의 부품·현장 인원도 미확정이다.
- **미정:** 실물 팔 사용 가능일을 언제 확정할지, 어떤 날짜·검증 결과에서 손 단독 시연에
  집중할지, 축소안 준비 시간을 얼마나 확보할지, 보유품 설치와 현장 운영 가능 여부.
- **판단:** 팔 사용 제약이 계속 늦어지면 축소안의 검증·리허설 시간도 소진될 수 있다.
- **해결 확인:** 실물 접근 가능성과 검증 진척에 따른 판단 시점을 기록한다.
  축소안을 기본 팔·손 MVP 완료로 보고하지 않는 기존 원칙은 유지한다.

### 9-7. 문서 표현과 과거 기록의 혼동

- **범위 안내 정정:** 첫머리의 ‘§1~§7 확정’ 표현을 기본 기능, 제안 일정·기준,
  미결정 항목으로 구분했다. 이 문구 정정이 위 미해결 사항의 해결을 의미하지 않는다.
- **남은 문서 부채:** `docs/SIM2REAL_ROADMAP.md` 첫머리에는 웹캠 3대 취소 안내가 있으나,
  본문의 3D 비전 표와 연결 구성도 등에는 이전 표현이 남아 있다. 해당 절이나 검색 결과만
  읽으면 현재 보유 장비로 오해할 수 있다. 이번 기록에서는 로드맵 본문을 일괄 수정하지 않았다.
- **현재 기준:** 웹캠 1대+D405 1대, 추가 구매 불가. D405 영상 표시·자동 접근은 추가 계획이다.

## 참고 문서

- [실물 연결·장기 로드맵](docs/SIM2REAL_ROADMAP.md): 이번 전시 범위는 본 문서를 따른다.
- [팔·손 브리지](docs/modules/REAL_BRIDGES.md)
- [MediaPipe 텔레옵](docs/modules/VISION_TELEOP.md)
- [비전 현황과 포트 충돌](vision/README.md)
- [장기 RL 관찰 재설계](docs/RL_POLICY_REDESIGN.md)
- [SuperDex 후속 작업](docs/SUPERDEX_POC_PLAN.md)
