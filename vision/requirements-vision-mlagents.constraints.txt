# Python
# Python == 3.10.12 (3.10.4로도 동작 확인 — mlagents는 3.10.x 전용, 패치버전은 무관)
# 2026-07-14 vision/.vision에서 검증한 비전+ML-Agents 공용 venv 제약.
# pip freeze 전체 스냅샷은 requirements-vision-mlagents.resolved.txt 참고.

# ML-Agents / MediaPipe 공통 핵심 의존성
numpy==1.23.5
protobuf==3.20.3
grpcio==1.53.2

# ML-Agents
onnx==1.15.0
tensorboard==2.14.1
# torch==2.1.1+cpu 였음(2026-07-14) — 당시 본학습은 별도 CUDA VDI에서 돌렸고 이 로컬 venv는
# 스모크테스트 전용이라 CPU로 충분했다(docs/DG5F_GRASP_READY_REACH_HANDOFF.md 참고).
# 그 VDI가 없어져 본학습도 이 로컬 RTX 2080에서 해야 하므로 CUDA 빌드로 전환(2026-08-25).
# 드라이버 CUDA 12.3(nvidia-smi) 기준 cu121 휠 사용: pip install torch==2.1.1+cu121
#   --index-url https://download.pytorch.org/whl/cu121
torch==2.1.1+cu121
h5py==3.10.0
Pillow==10.2.0
PyYAML==6.0.1
six==1.16.0
cattrs==1.5.0
attrs==23.2.0
huggingface-hub==0.20.3

# ML-Agents Env API
cloudpickle==3.0.0
gym==0.26.2
pettingzoo==1.15.0
filelock==3.13.1

# MediaPipe
mediapipe==0.10.11
opencv-contrib-python==4.8.1.78
absl-py==2.1.0
flatbuffers==23.5.26
sounddevice==0.4.6

# MediaPipe가 가져오는 JAX 계열을 NumPy 1.23.5 호환 버전으로 제한
jax==0.4.23
jaxlib==0.4.23
scipy==1.10.1
ml-dtypes==0.2.0
matplotlib==3.7.5

# 기존 Vision 코드에서 사용할 가능성이 높은 데이터 처리 패키지
pandas==2.0.3
