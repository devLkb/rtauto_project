# 구현해야 하는 것 — 단일 목록

마지막 갱신: 2026-09-23 (세션 2 — A-9 대상 고르는 규칙 구현, 실물 D405·URSim 으로 확인).

이 파일은 **"앞으로 뭘 만들어야 하는가"의 목록**이다. 근거·수치·설계는 여기 적지 않고
정본 문서를 링크한다(이유는 [`README.md`](README.md) "쓸 때 지키는 것").
날짜별 작업 기록은 [`daily/`](daily/) 에 있다.

## 상태 표시

| 표시 | 뜻 |
|---|---|
| ⬜ | 아직 시작 안 함 |
| 🔵 | 진행 중 — 어느 날짜 문서에서 손대고 있는지 링크를 단다 |
| ✅ | 끝남 — 끝난 날짜와 근거 링크를 단다 |
| ⏸ | 일부러 미뤄 둠 — 왜 미뤘는지 한 줄 |
| 🔒 | 사용자 승인을 받아야 착수 가능 |

> **규칙:** 항목을 끝냈으면 상태를 ✅ 로 바꾸고 **그날 날짜 문서를 링크**한다.
> 새 항목을 넣을 때는 **왜 필요한지 한 줄**과 **끝났다고 볼 수 있는 조건**을 같이 적는다.
> 조건이 없으면 끝났는지 아무도 판단할 수 없다.

---

## A. 11월 전시 (AI Festa) — 기한이 있는 유일한 묶음

정본: [`ai_festa_plan.md`](../ai_festa_plan.md). 확정 범위는 §1~§5,
아래 항목들은 §9 "실행 전 명확히 할 미해결 항목"에서 온 것이다.
**이건 새 기능 요구가 아니라, 이미 합의된 시연을 실제로 굴리려면 비어 있는 자리다.**

| 상태 | 항목 | 끝났다고 볼 조건 |
|---|---|---|
| ✅ | **A-1. 팔에 명령을 보내면서 동시에 실제 관절 상태를 화면에 보여주기** (§9-1) — [2026-09-21](daily/2026-09-21.md) 세션 4. `PicknPlaceArmJointPanel.cs`의 `DrawUrsimSection()`이 `"  /  URSim {실제각}°"` 문구로 명령각·실제각을 나란히 표시. Unity Play 모드로 URSim(5.25.2)과 실제 연결해 관절 슬라이더를 조작하며 사용자가 직접 화면으로 확인함 — 두 값이 서로 안 덮어쓰고 각자 갱신됨 | 끝남. 사용자가 Unity에서 URSim 연결한 채로 조작하며 화면을 직접 확인했고, 서로 안 덮어쓰는 것을 봤다 |
| ⬜ | **A-2. "잡은 상태 유지"가 정확히 무슨 값을 붙잡는 것인지 정하고 구현** (§9-2) — 마지막 사용자 목표각 / 브리지가 제한한 명령각 / 실제 관절각이 서로 다르다 | 들어 올리는 동안 손 상태 표시가 계속 갱신되고, 손 추적이 끊긴 것과 일부러 고정한 것이 화면에서 구분된다 |
| ⬜ | **A-3. 팔이 따라갈 경로를 만들고 저장하고 실행하는 방법 정하기** (§9-3) — 지금 `arm/` 은 관절 명령 전달만 하고 경로 계획은 없다. **2026-09-21 코드 리뷰 3차로 절반 진전**: `ArmMover.move_to()`가 이제 `MoveResult.arrived`(실제 관절값을 읽어 목표와 비교)를 돌려줘 "명령 전송 완료"와 "실제 도착"을 구분하는 조건은 채워졌다. **아직 안 된 것**: 경로 자체를 재현 가능하게 저장하는 부분(실행한 자세·결과를 파일로 남기기)은 손대지 않음 | 경로가 재현 가능하게 저장되고, 명령 전송 완료가 아니라 **실제 도착**을 확인한 뒤 다음 단계로 넘어간다 |
| ⬜ | **A-4. 취소·실패·재연결 때 팔과 손을 각각 어떻게 할지 정하기** (§9-4) — 물체를 들고 있는 중 취소가 지금은 정의돼 있지 않다. 2026-09-21 코드 리뷰 3차로 `PicknPlaceArmJointPanel.FullReset()`이 초기화 중 Unity→URSim 송신을 강제로 끄도록 고쳐졌지만(안전장치일 뿐), A-4가 요구하는 **취소·실패·재연결 각각의 팔/손 처리 정책**을 정한 것은 아니다 — 범위 그대로 남음 | 단계별 실패 상황마다 팔·손 처리와 재개 조건이 정의되고 실물에서 확인된다 |
| ⬜ | **A-5. 합격 기준 숫자 확정** (§9-5) — 지연 허용치, 들기 높이, 한 번의 성공이 어디서 시작해 어디서 끝나는지 | 반복 시험 **전에** 측정 항목·조건·합격선이 문서에 적힌다 |
| ⬜ | **A-6. 실물 팔을 못 쓸 때 축소안(손 단독)으로 갈아타는 시점 정하기** (§9-6) | 언제까지 실물 팔 사용 가부를 확정할지, 어떤 결과면 축소안으로 가는지 날짜로 적힌다 |
| 🔵 | **A-9. 여러 물체 중 어느 것으로 갈지 고르는 규칙** — **2026-09-23 세션 2 에서 ①②③ 구현, 실물 D405 + URSim 으로 확인** ([2026-09-23](daily/2026-09-23.md)): `arm/target_selection.py` 신설. 거절되면 다음 후보 / 3 cm 넘게 가까워야 바꿈 / `Tracker` 번호(팔 이동을 시도하면 버림) / 잠깐 안 보여도 3장면 기다림 / 사람이 취소한 물체는 다시 안 고름 / 고른 이유·건너뛴 이유를 터미널·화면에 표시. 별도 검토 반영, 시험 25개. **남은 것**: 팔을 실제로 움직인 뒤 다시 고르는 흐름(`--move --watch`)은 아직 안 돌려 봄. 후보가 받아들여지는 경우의 관절각 풀기 넘어가기는 시험(가짜)으로만 확인함(책상 위 카메라라 전부 작업 공간 밖으로 거절됨). 아래는 등록 당시 설명 — 2026-09-22 세션 9 에서 구현하면서 드러난 약점 3가지. 지금 규칙은 `arm/approach_object.py`의 `observe()` 네 줄이 전부다: YOLO 가 찾은 것만 후보 → `--target` 이름 거르기 → **카메라에서 가장 가까운 것** 하나. <br>**① 1등이 거절당하면 그냥 포기한다** — 가장 가까운 물체가 작업 공간 밖이거나 팔이 못 닿으면 2등을 시도하지 않고 끝난다. 물체 여러 개를 놓는 시연에서 바로 드러난다. <br>**② '가장 가까운 것'이 흔들린다** — 두 물체의 거리가 비슷하면 장면마다 대상이 바뀐다. `--watch` 로 반복하면 팔이 둘 사이를 오갈 수 있다. <br>**③ 같은 물체인지 추적하지 않는다** — `vision/d405/segment_objects.py`에 **이미 `Tracker` 가 있는데**(장면이 바뀌어도 같은 물체에 같은 번호를 준다) `observe()` 가 안 쓴다. 매 장면 처음부터 다시 고른다. <br>실측 예: 종이컵 93 %(0.24 m)와 주스병 83 %(0.22 m)가 같이 보일 때 **주스병**을 골랐다 — 확신이 높아서가 아니라 2 cm 더 가까워서다 | 거절당한 후보 다음으로 넘어가고, 같은 물체를 장면이 바뀌어도 같은 물체로 알아보며, 왜 그 물체를 골랐는지가 화면에 나온다 |
| ⏸ | **A-7. D405 근접 영상 Unity 표시** (§8-1) — 기본 시연 범위 **밖**. 기본 시연이 안정된 뒤에 판단 | — |
| 🔵 | **A-8. D405로 물체 위치를 재서 팔이 자동 접근** (§8-2) — ⚠️ **2026-09-21 정정: 현장 목표에 포함된다** (예전엔 "기본 시연 범위 밖"이라고 적었는데 틀렸다 — `ai_festa_plan.md` §0 정정 참고). D 묶음(파지 자세 스스로 정하기)의 결과물이 이 항목의 실질적 구현 경로다. **2026-09-22 세션 9 — 코드 경로가 처음으로 끝까지 이어졌다**: `vision/d405/object_pose.py`(물체 위치·방향 재기) + `arm/pregrasp_planner.py`(물체 앞에 설 자리 + 안전 검사 7가지) + `arm/approach_object.py`(전체를 순서대로 부르는 도구)를 새로 만들고, 나머지는 기존 파일을 그대로 썼다. 시험 48개 추가, 총 193개 통과. **같은 날 실물로 끝까지 확인함**: 실물 D405 로 종이컵 93 %·주스병 83 % 인식, 주스병 크기 11.7x7.4x5.7 cm 측정, Camera→TCP→Base 세 좌표 출력, UR 내장 IK 로 관절각 풀림, **URSim 에서 실제 이동·도착 확인(최대 관절 오차 0.000°)**. 실물 카메라가 버그 2개를 잡아냈다(카메라 기준에서 위아래를 판정하던 것, `--plan` 이 관절각을 안 풀어 보던 것). ⚠️ **손목 카메라 장착값이 아직 0이라 로봇 기준 좌표는 여전히 못 믿는다** — D-7 이 남은 유일한 임계경로다. 상세: [2026-09-22](daily/2026-09-22.md) 세션 9 | 사람이 지정한 배치 구역 안에서 D405로 물체를 찾아, 팔이 파지 가능한 자세까지 스스로 이동해 정지하는 것이 실물에서 반복 확인된다 |

---

## B. 파지 강화학습 (SuperDex)

정본: [`docs/SUPERDEX_POC_PLAN.md`](../docs/SUPERDEX_POC_PLAN.md) §0 인수인계 블록.
계약(무엇을 보고 무엇을 움직이는지)의 정본은
[`docs/RL_POLICY_REDESIGN.md`](../docs/RL_POLICY_REDESIGN.md).

> 파이썬 환경이 다르다 — 이 묶음은 `superdex/.venv/`(파이썬 3.12)를 쓴다.
> 나머지 리포는 3.10.11 이다. 섞으면 안 된다.

| 상태 | 항목 | 끝났다고 볼 조건 |
|---|---|---|
| ⏸ | **B-1. 물체별 사람 상수(`place`) 없애기** — 2026-09-17 에 **접근 방식이 바뀌었다.** 팔을 학습시켜 스스로 다가가게 하는 대신, **자세 후보를 훑어서 채점표를 만들고 그걸 예측기에 가르치는** 방식으로 같은 목적을 이룬다 | D-2·D-4 로 이관 |
| ⬜ | **B-2. 게이트 1(세 군데서 같은 답이 나오는지) act26 재검증 — Unity 쪽 한 다리만 남았다.** 파이썬 두 다리는 통과했다(0.000e+00 / 1.907e-06). 개인 노트북에 Unity 가 없어서 못 돌렸다 | `unity/Assets/Editor/PolicyParityCheck.cs` 에 `dg5f-grasp-2` 입력을 넣어 오차가 기존 수준(1e-06 안팎)으로 나온다 |
| ⬜ | **B-3. 12종 도형을 섞어서 학습시키고, 학습에 안 쓴 도형으로 넘어가는지 보기.** 도형 15종이 전부 잡히는 것은 2026-09-14 에 확인됐다 — 다음은 섞어 학습이다 | 학습에 쓰지 않은 도형에서 파지 성립률이 숫자로 나온다 |
| ⬜ | **B-4. 학습 속도(throughput) 병목 대응.** 실효 약 470 steps/s. 물체 다양성 + 26축 + 조건 흔들기가 겹치면 여기서 막힌다 — B-3 을 돌리면 바로 부딪힌다 | 병목 지점이 측정되고, 늘릴 수 있는지 없는지 판정된다 |
| ⬜ | **B-5. 점군(카메라가 보는 점 덩어리) 합성 시제품** — 선생/학생 구조의 2단계. 아직 한 번도 실측하지 않은 최대 미검증 항목 | 시뮬레이터에서 만든 점군으로 학생 쪽 입력이 성립하는 것을 확인 |
| 🔒 | **B-6. 실물 규모로 올리기 / 손목 자세까지 움직이기** — 사용자 승인이 아직 없다. 승인 없이 착수하지 않는다. ⚠️ **"시뮬레이션에서 배운 것을 실물로 옮기기"는 2026-09-17 에 승인됐다** — 아래 D 묶음으로 분리 | — |
| ⬜ | **B-7. 시뮬레이터가 손 20관절 내부 순서를 검사하지 않는다** — `superdex/envs/dg5f_grasp_env.py` 는 관절 이름에 `dg5f` 가 들어있는지만 본다. 지금은 URDF·실물 SDK·Unity 와 우연히 맞아 있지만, 순서를 손대거나 외부 정책을 꽂으면 **조용히 어긋나고 아무도 모른다**. 2026-09-18 조사 중 발견 ([`docs/EXTERNAL_GRASP_POLICY_SURVEY.md`](../docs/EXTERNAL_GRASP_POLICY_SURVEY.md) §6) | 환경이 뜰 때 손 20관절 이름·순서를 URDF 정본과 대조하고, 다르면 값을 쓰기 전에 멈춘다 |
| 🔵 | **B-8. 파지 성공 판정이 손끝(지문) 5곳만 본다 — 손끝 아닌 부위에 물체가 끼어도 "성공"으로 센다** — 2026-09-22 사용자가 종이컵 옆 파지 시연을 보다가 "손가락이 아니라 관절에 낀 채로 버틴다"를 눈으로 지적, 새 진단 도구(`superdex/scripts/diagnose_wedge_contact.py`)로 실측하니 손끝 아닌 관절에 **최종 유지 상태에서도 수백~3,435 N**(종이컵 무게 대비 물리적으로 말이 안 되는 크기)이 걸려 있었다 — 관통/솔버 불안정으로 추정(1차 가설). 10/10 에피소드에서 재현됨. D-2 채점표(`pose_score_*.json`)의 `rate`가 이걸 전혀 못 거른다는 뜻이라 **D-4(예측기 학습)가 낀 자세를 정답으로 배우고 있었을 가능성**이 있다. ⚠️ 원래 게이트 0/2/4 검증(증거3·4, 파지 성립률 100%)은 duck_lamp 기본 배치 기준이라 이 문제와 범위가 다를 수 있음 — 아직 미확인. 상세: [2026-09-22](daily/2026-09-22.md) 세션 1 | duck_lamp 기본 자세에서도 같은 현상이 있는지 가르고, D-2 채점 기준에 손끝 아닌 관절힘 상한을 넣을지 결정하고, 필요하면 D-4를 다시 학습시킨다 |

---

## C. 문서·환경 정리

| 상태 | 항목 | 끝났다고 볼 조건 |
|---|---|---|
| ⬜ | **C-1. 로드맵 본문에 남은 "웹캠 3대" 표현 정리** ([`ai_festa_plan.md`](../ai_festa_plan.md) §9-7). 맨 앞에 취소 안내는 붙였지만 본문 표·구성도에는 옛 표현이 남아 있어, 그 절만 읽으면 지금 장비로 오해한다 | 로드맵 본문에서 현행 장비(웹캠 1대 + D405 1대)와 어긋나는 서술이 없다 |
| ✅ | **C-2. 이 인수인계 체계(`claudeDocs/`) 정착** — [2026-09-16](daily/2026-09-16.md) 시작, [2026-09-18](daily/2026-09-18.md) 세션 17 에서 성립 | 끝남. 2026-09-18 세션 17 이 **다른 컴퓨터(리눅스)에서 사용자 설명 없이** `claudeDocs/` 만 읽고 세션 1~16 을 이어받았다 |
| ⬜ | **C-3. 리포 루트 `README.md` 전면 개정** — "현재 상태 / 알려진 이슈"가 2026-07-20 기준이고 `training/` 을 아직 `DG5FGraspPointReach` 로 설명한다. 2026-09-16 docs 정리 중 발견 | 새 컴패에서 루트 README 를 그대로 따라 해도 막히지 않는다(원칙 2) |
| ⬜ | **C-4. 파이썬 3.12(`superdex/`)와 3.10.11(`vision/`·`arm/`)이 한 프로그램에 못 들어간다** — 카메라로 보고 예측기가 자세를 정해 실제 팔을 움직이는 순간 **반드시 부딪힌다**. 지금까지 정면으로 다룬 적이 없다. 2026-09-18 조사 중 발견 ([`docs/EXTERNAL_GRASP_POLICY_SURVEY.md`](../docs/EXTERNAL_GRASP_POLICY_SURVEY.md) §4). 여전히 안 풀림 — 2026-09-21 코드 리뷰 3차에서 새로 만든 `superdex/scripts/candidate_pose_generator.py`/`camera_frame_transform.py`는 numpy만 쓰므로(torch 불필요) 이 경계를 넘기가 상대적으로 쉬운 후보이긴 하나, 실제로 3.10.11 쪽 프로세스(`arm/`)에서 부르는 배관은 아직 없다 | 두 환경이 각자 제 파이썬으로 뜬 채, 한쪽이 낸 자세를 다른 쪽이 받아 팔을 움직이는 것이 한 번 성립한다. 주소·포트는 `config/rtauto_config.py` 에서 온다(원칙 1) |

---

## D. 파지 직전 자세를 스스로 정하기 (2026-09-17 착수 결정)

정본: [`docs/FESTA_PREGRASP_PLAN.md`](../docs/FESTA_PREGRASP_PLAN.md).
사용자 결정으로 **학습한 예측기(길 2)** 로 간다. 근거와 위험은 정본 문서 참고.

> ⚠️ **2026-09-21 정정 — 이 묶음은 이제 11월 전시 현장 목표에 들어간다.** 예전엔
> "부스에 제출한 계획안이 손→그리퍼까지이고 자동 접근은 약속돼 있지 않으니 전시 마감과
> 무관하다"고 적었는데(2026-09-17 사용자 확인 근거), **그 근거 자체가 틀렸다** — 원래
> 하려던 것이 바로 이 묶음(3D 카메라로 물체를 찾아 팔이 파지 가능 자세까지 스스로
> 이동)이었다. 대외 제출 문구는 여전히 손→그리퍼까지지만(안전한 방향), 현장 목표
> 마감(2026-11-13)에는 이 D 묶음이 걸려 있다. 자세한 경위는
> [`../ai_festa_plan.md`](../ai_festa_plan.md) 맨 앞 "2026-09-21 정정" 참고.

| 상태 | 항목 | 끝났다고 볼 조건 |
|---|---|---|
| ✅ | **D-1. 주고받는 값 확정** — `contracts/grasp_prepose.py`. [2026-09-17](daily/2026-09-17.md) 세션 4·6 | 끝남. 두 방식이 같은 자료구조를 채우고, 그 값으로 가짜 팔이 실제로 움직였다 |
| ✅ | **D-2. 채점표 만들기** — [2026-09-17](daily/2026-09-17.md) 세션 7·8, [2026-09-18](daily/2026-09-18.md) 세션 1·3(기울기 추가, 중간 저장·`--resume`). **2026-09-21 정정: 표는 끝남** — 13종 물체 × 468칸, 36분 만에 완주. BACKLOG의 "돌리는 중"은 낡은 기록이었다 | 끝남. 형상 13종 × 자세 여러 개의 성공·실패 표가 파일로 남았다 |
| ⬜ | **D-3. 점 덩어리 합성 실측** — **이 길의 최대 미검증 항목** | 표면 메시 + 시야 자르기 + 가림 판정으로 "카메라가 볼 법한 점"이 나오는 것을 확인 |
| 🔵 | **D-4. 예측기 학습** — D-2 채점표의 좋은 자세로 세 차례 학습 시도([2026-09-18](daily/2026-09-18.md)). **2026-09-21 정정: 멈춰 둔 상태** — 세 번 다 "2줄짜리 기하학 계산" 어림값을 못 이긴다(1등 맞히기 약 15%, 3등 안에 들기 약 62%로 둘이 비슷). 원인은 찾음 — 예측기의 물체 모양 처리 갈래가 자세 후보 정보를 아예 못 본다. 손 위치를 손바닥 기준으로 바꾸는 수정은 했지만 그걸로 다시 학습시킨 결과는 아직 기록 없음. <br>⚠️ **2026-09-21 코드 리뷰 3차로 구조적 문제 하나 더 확인**: 지금 predictor는 "점 구름 → 자세" 생성기가 아니라 **"(점 구름, 후보 자세) → 점수"** 채점 모델이다(`dataset_contract.py` P1-7). 따라서 실물에서 쓰려면 **점 구름만 보고 후보 자세를 만드는 별도 생성기**가 필요하다 — 최소 인터페이스+시험용 구현을 `superdex/scripts/candidate_pose_generator.py`(P1-8)로 새로 만들었으나, D-2 채점표를 대체해 실제 학습 데이터를 만드는 배관은 아직 없다. 또한 `build_pose_dataset.py`를 카메라 시점 기준으로 다시 설계하려면(현재는 물체 기준=leaky, `frame="object"`) **카메라 시점마다 후보 자세도 그 시점 기준으로 새로 변환해야** 한다 — 그냥 라벨만 바꾸면 안 됨. 이 좌표 변환 자체는 `superdex/scripts/camera_frame_transform.py`(P1-9)로 순수 계산만 떼어 만들고 시뮬레이터 없이 시험했지만(`superdex/tests/test_camera_frame_transform.py`), `build_pose_dataset.py`의 실제 촬영 루프에 엮는 것은 시뮬레이터 실행이 필요해 안 함. **그 시험이 찾아낸 추가 문제**: `train_pose_predictor._features()`가 돌려주는 자세 특징(pose_f)은 `pose_quat`을 그대로 쓰는 **절대** 방향이라, 카메라 시점별 후보로 바꾸면 같은 물리적 자세인데 시점에 따라 값이 달라진다 — 카메라 기준 재설계 시 `_features()`도 같이 재검토 필요(시험명: `test_pose_feature_is_NOT_frame_invariant_known_gap_for_camera_frame_redesign`) | 학습에 안 쓴 형상에서도 좋은 자세를 내놓는 것이 숫자로 확인되고(계약 R3), 그 숫자가 기하학 어림값보다 낫다 |
| ✅ | **D-5. 자세 → 관절각 계산 → 가짜 팔(URSim) 이동** — `arm/prepose_to_joints.py`. [2026-09-17](daily/2026-09-17.md) 세션 5·6 | 끝남. 도달 오차 **0.0000 mm / 0.00000 deg**, 못 닿는 자세는 **0.000000 deg** 움직임으로 거절 |
| ✅ | **D-6. 손목 카메라 장착값 설정 자리 뚫기** (원칙 1) — `config/rtauto_config.py` D405_MOUNT_*, `arm/eye_in_hand.py`, 시험 17개. [2026-09-18](daily/2026-09-18.md) 세션 5 | 끝남. 0값으로도 경로가 돌고, **0이면 "아직 안 쟀다" 고 경고를 함께 돌려준다** — 조용히 틀린 좌표가 쓰이지 않게 |
| 🔵 | **D-9. 흰 종이컵처럼 거리값이 안 나오는 물체 버티기** — [2026-09-18](daily/2026-09-18.md) 세션 11~16, [2026-09-21](daily/2026-09-21.md) 세션 5~6. **실물 확인됨**: 컵 3개+캔이 각각 잡히고, 큰 컵 입구 지름 8 cm 를 **8.1 cm** 로 쟀다. 좌표 어긋남·점 소비 표시 버그 2건 수정, 여러 장 합치기, 테두리로 크기 추정, 조각 합치기. **2026-09-21 세션 5 진짜 원인 고침**: `yolo_assist.py`가 "계측에 안 쓴 점"을 "다른 물체의 점"과 같은 뜻으로 처리해 같은 물체를 또 만들던 버그를 `used[idx_in]=True`로 수정, 시험 3개 추가. **2026-09-21 세션 6 범위 축소**: 모양(geometry) 안전망 자체를 **기본값 끔**으로 바꿨다(`--geometry-fallback` 로 켤 수 있음) — 지금 목표는 "상식적 범위"를 YOLO로 인식하는 것이고, 안전망이 만들던 중복(페트병이 여러 조각으로 쪼개짐)은 **미지 형상 일반화(페스타 이후 별도 과제)** 로 이월했다. 기본값(YOLO만)에서는 중복 문제 자체가 발생하지 않는다 | 기본값(YOLO만) 기준으로 같은 물건이 한 번만 세어진다. `--geometry-fallback` 켰을 때의 쪼개짐은 이 항목의 완료 조건이 아니다(페스타 이후 과제로 이월) |
| 🔵 | **D-10. 흰 컵이 안 잡히는 게 "빛이 모자라서" 인지 "무늬가 없어서" 인지 가르기** — 둘은 정반대 처방인데 아직 구분 못 했다. [2026-09-18](daily/2026-09-18.md) 세션 18 이 최우선으로 지목. **2026-09-21 세션 5**: `vision/d405/exposure_sweep.py` 신설 — 이 카메라의 실제 노출/게인 허용 범위를 SDK에서 읽어 표시(범위 밖 값은 조용히 안 자르고 건너뜀), 노출마다 RAW/FILTERED 나란히 측정 + 색·좌우 원본 영상 저장, CSV 기록, 끝나면 설정 복원. 실물 카메라로 스모크 테스트(크래시 없음, 파일·CSV 정상)까지 함. ⚠️ **정식 실험(흰 종이컵을 실제로 놓고 노출 훑기) 은 아직 안 함** — 도구만 준비됨 | 640x480·게인 16 고정으로 노출 1,000~30,000 µs 를 훑고, **컵의 좌우 원본 영상**과 날것 깊이를 같이 봐서 둘 중 어느 쪽인지 답이 나온다 |
| ⏸ | **D-7. 실물 — 카메라 취득·손목 위치 맞추기·오차 대조·사람 판정** | 장비 도착 대기. ⚠️ **손목 카메라 위치 맞추기가 진짜 임계경로** — 해 본 적 없는 작업. 2026-09-21 코드 리뷰 3차로 의존 항목이 하나 더 늘었다: D-7이 끝나 `RTAUTO_D405_MOUNT_VALIDATED=1`이 되기 전까지는 `arm/eye_in_hand.py`의 `require_validated_mount_for_physical_move()`가 실물 이동 경로를 막는다(P1-5, 2026-09-21) — 이는 의도된 동작이지 버그가 아니다 |
| ✅ | **D-8. 잡은 뒤 물체가 돌아간 각도(기울기) 재기** — [2026-09-18](daily/2026-09-18.md) 세션 1 | 끝남. 채점표 각 칸에 `tilt_median_deg` 가 남는다. **성공 판정에는 아직 안 넣는다** — 기준 각도는 분포를 보고 정한다 |

### E. 실물에서 잡기 — 손끝 센서 없이 (2026-09-18 착수)

설계 정본: [`docs/GRASP_CONTACT_DETECTION.md`](../docs/GRASP_CONTACT_DETECTION.md)

| 상태 | 항목 | 끝났다고 볼 조건 |
|---|---|---|
| ✅ | **E-1. 접촉 판단 규칙 + 순서도 + 시험** — `hand/grasp_contact.py`, `hand/grasp_sequence.py`, `hand/measure_baseline.py`. [2026-09-18](daily/2026-09-18.md) 세션 2 | 끝남. 손 없이 도는 시험 17개 통과. **실물 검증 0회** |
| ⏸ | **E-2. 무부하 전류 실측** — `hand/measure_baseline.py --ip` | 장비 도착 대기. 관절 20개 × 쓰는 각도 구간이 전부 채워지고 "없음 — 쓸 수 있다" 가 나온다 |
| ⏸ | **E-3. 기준값 맞추기** — 지금 값(전류 배수·오차·속도·시간)은 **전부 짐작** | E-2 의 실측값으로 맞추고, 왜 그 값인지 문서에 적는다 |
| ⏸ | **E-4. 물체 하나로 닫기 실험** — 닿은 손가락 수가 맞는지 **눈으로 확인**(원칙 6) | E-3 |
| ⏸ | **E-5. 팔과 붙여 들어올리기·확인** — 집어보기 + 전류 + 팔 힘센서 | E-4 |
| ⬜ | **E-6. 브리지에 속도 켜기** — `RECEIVED_DATA_TYPE` 에 VELOCITY(4) 추가 | 지금 `[1,2,7,...]` 이라 속도가 0 으로만 들어온다. 실물에서 값이 들어오는 것을 확인 |
| ⬜ | **E-7. 전류 상한을 걸고 쥐는 방식과 비교** — `SetCurrentControlMode` / `SetGraspData` | E-5. 우리가 직접 조이는 것보다 나은지 판정 |

**순서**: ~~D-1 → D-5 → D-2~~(끝남) → **D-4**(멈춰 둔 상태 — 재개하거나 D-3부터 다시 볼지 결정 필요) → D-3.

> 🛑 **팔 6축 학습(`arm_hand26`)은 필수 경로에서 뺐다**(2026-09-17).
> 확인해 보니 **팔을 포함한 학습은 한 번도 돌아간 적이 없다** — 학습 결과가 전부
> 2026-09-09 이전인데 팔 6축 구현은 2026-09-10 이다. 그리고 "보고 → 정하고 → 가고 →
> 다시 보기" 방식에는 팔 학습이 필요 없다(팔은 목표까지 계산으로 간다).
> 환경은 그대로 두고, "가면서 고치기"가 꼭 필요해지면 그때 되살린다.

---

## 최근에 끝난 것

되돌아볼 때 쓰라고 남긴다. 너무 길어지면 오래된 것부터 지운다.

| 끝난 날 | 항목 | 근거 |
|---|---|---|
| 2026-09-21 | **YOLO 자체 중복 검출(같은 물체가 cup/bottle 등으로 겹쳐 잡힘) 수정 — 실물로 확인됨** — `suppress_cross_class_duplicates()` 신설, class 이름 일치 여부는 안 보고 포함 비율(IoS)+거리 일치로만 판단하도록 일반화(처음엔 "같은 class는 안 건드림"이었으나 실측으로 뒤집힘) | [2026-09-21](daily/2026-09-21.md) 세션 7 |
| 2026-09-21 | **A-1 — 명령각·실제각 나란히 표시, 조작 중 실제로 확인됨** | [2026-09-21](daily/2026-09-21.md) 세션 4 |
| 2026-09-18 | **인수인계 체계가 실제로 성립했다 (C-2)** — 다른 컴퓨터에서 설명 없이 이어받음 | [2026-09-18](daily/2026-09-18.md) 세션 17 |
| 2026-09-17 | **D-1·D-5 — 파지 직전 자세를 주고받고, 그 자세로 팔이 움직여 멈춘다** (가짜 팔에서 확인) | [2026-09-17](daily/2026-09-17.md) 세션 6 |
| 2026-09-17 | **파지 직전 자세 결정 방식 확정** — 학습한 예측기로 가기로 결정, 계획 수립 | [`docs/FESTA_PREGRASP_PLAN.md`](../docs/FESTA_PREGRASP_PLAN.md) / [2026-09-17](daily/2026-09-17.md) |
| 2026-09-17 | **AI Festa 참가 계획서·제품 소개 PDF 제출** (마감 2026-09-18보다 하루 먼저) | [`ai_festa_plan.md`](../ai_festa_plan.md) §6 확정 일정 / [2026-09-17](daily/2026-09-17.md) |
| 2026-09-16 | `docs/` 정리 — 구세대 문서 6개를 archives 로, 문서 입구(`docs/README.md`) 전면 개정 | [2026-09-16](daily/2026-09-16.md) 세션 2 |
| 2026-09-16 | 웹캠 여러 대일 때 시작할 때 창에서 직접 고르기 + 화면 크기·프레임률 자동 선택 | 커밋 `a82a514` `fb41610` `3a6b067` `a9f7f8e` `ab5433b` |
| 2026-09-16 | 새끼손가락 벌림이 로봇에 전달되지 않던 문제 해결(실제 손 녹화로 검증) | 커밋 `3352866` `a986687` `fd95e12` |
| 2026-09-14 | 도형 12종 전부 잡히는 것 확인 — 쓸 수 있는 물체가 3종 → 15종 | [`SUPERDEX_POC_PLAN.md`](../docs/SUPERDEX_POC_PLAN.md) "shape_box 오라클 스윕" |
| 2026-09-10 | 선생/학생 구조 승인 + 팔 6축 포함(act26) 구현 | [`SUPERDEX_POC_PLAN.md`](../docs/SUPERDEX_POC_PLAN.md) §0 |
