# 2026-09-16

이어받는 세션은 맨 위 "현재 상태"와 맨 아래 "다음에 할 일"만 읽어도 된다.

## 현재 상태

브랜치 `main`, 원격과 같은 지점. 작업 트리 깨끗함.
오늘 오전까지 **웹캠 손동작 인식 쪽 작업**이 끝나 커밋됐다(카메라 고르기, 화면 크기 자동
선택, 새끼손가락 벌림 수정). **파지 강화학습(SuperDex) 쪽은 2026-09-14 이후 손대지 않았고**,
다음 착수점은 그대로 "물체별 사람 상수(`place`) 없애기 학습 검증"이다
([`BACKLOG.md`](../BACKLOG.md) B-1).
11월 전시 준비는 아직 §9 미해결 항목이 그대로 열려 있다(A-1 ~ A-6).

## 세션 1 — 인수인계 체계 만들기

### 왜 했는가

사용자 요청. 이 프로젝트를 한 사람이 맡고 있어 AI 의존이 클 수밖에 없는데, 작업 컴퓨터
문제로 **대화가 도중에 비정상 종료되는 일이 잦다.** 그때마다 새 대화를 열고 상황을 처음부터
설명해야 했다. 그 설명을 파일로 대신하기 위해 `claudeDocs/` 를 만들었다.

### 한 일

- `claudeDocs/README.md` — 이 체계의 규칙. 세션 시작 때 읽는 순서, **작업 중간마다 기록하라는
  규칙**(끝날 때 몰아 쓰면 끊겼을 때 통째로 날아간다), 날짜 문서 틀.
- `claudeDocs/BACKLOG.md` — 앞으로 구현해야 하는 것 한 곳에 모음. 11월 전시(A), 파지
  강화학습(B), 문서 정리(C)로 나눴다. 항목마다 **끝났다고 볼 조건**을 같이 적게 했다.
- `claudeDocs/daily/` — 날짜별 기록. 이 파일이 첫 번째다.
- `CLAUDE.md` 에 **원칙 5** 추가 — 모든 세션이 이 디렉터리를 읽고 쓰도록 강제.

### 판단한 것 (이어받는 사람이 알아야 함)

- **`claudeDocs/` 는 정본이 아니다.** 아키텍처·계약·실측값의 정본은 계속 `docs/` 다.
  여기에 수치를 복사해 두면 정본이 둘이 되어 곧 서로 어긋난다. 여기에는 "오늘 뭘 했고
  다음에 뭘 할지"만 쓰고 근거는 링크한다.
- `BACKLOG.md` 의 내용은 **기존 문서에서 뽑아 온 것**이다 — 11월 전시 항목은
  `ai_festa_plan.md` §9, 강화학습 항목은 `docs/SUPERDEX_POC_PLAN.md` §0 인수인계 블록.
  새로 지어낸 요구사항은 없다.

### 돌려본 것과 결과

| 명령 | 결과 | 판정 |
|---|---|---|
| `git status --short --branch`, `git log` | `main`, 작업 트리 깨끗 | 확인함 |

**코드는 건드리지 않았다.** 문서만 추가했으므로 실행해서 검증할 것이 없다.

### 사용자가 결정한 것

- `claudeDocs/` 디렉터리로 AI 세션끼리 인수인계하는 체계를 만든다 (요청).
- 한 파일에 전부 쌓지 않고 **날짜별로 문서를 나눈다** (요청).
- 구현할 기능 목록은 **문서 하나로** 유지한다 (요청).

### 막힌 것

없음.

### 커밋

- `2acdbc5` docs: 세션끼리 인수인계하는 claudeDocs/ 체계 신설 + CLAUDE.md 원칙 5
- 이 커밋 해시를 이 문서에 적은 후속 커밋이 하나 더 있다 — `origin/main` 으로 푸시했다.

## 세션 2 — `docs/` 정리

### 왜 했는가

사용자 요청("안 읽어도 되는 건 아카이브에"). 전수 조사하다 더 큰 문제를 찾았다 —
**`docs/README.md`(문서 입구) 자체가 틀려 있었다.** Unity 안의 파지 학습을 "현재 개발
기준"으로 안내하는데, 파지 학습은 2026-09-09 에 SuperDex 로 옮겨갔고 입구 문서에
**SuperDex 문서가 한 줄도 없었다.** 새 세션이 입구만 보고 폐기된 경로를 붙잡는 상태였다.

### 한 일

**구세대 문서 6개를 `docs/archives/` 로 옮겼다** (`git mv` 라 이력은 따라간다):

| 옮긴 문서 | 왜 |
|---|---|
| `DG5F_GRASP_LIFT.md` | 구세대 GraspLift(UR5e + 왼손) 계약 |
| `DG5F_TOPDOWN_TRANSFER.md` | 구세대 `DG5FGrasp` 기반, 2026-07-25 이후 손대지 않음 |
| `V2_TRAINING_FAILURE_ANALYSIS_20260717.md` | 7월 실패 학습 분석, 역사 기록 |
| `docs2/GRASP_LIFT_발표자료.md` · `docs2/GRASP_LIFT_설명.md` | 구세대 발표·설명 |
| `docs2/기술선택_설명.md` | UR5e 전제 + "파지 학습은 Unity 가 맡는다" 전제. 둘 다 바뀜다 |

**옮기지 않은 것 2개** — 구세대인데도 남겨둔 이유가 있다:
`DG5F_PICKNPLACE.md` 와 `TRAINING_RUN_LEDGER.md` 는 **지금 돌아가는 `training/scripts/` 의
상수·합격기준이 근거로 인용**한다. 대신 맨 위에 "이전 세대이고 왜 남겨됀는지" 배너를 붙였다.

**다시 쓴 문서 3개**: `docs/README.md`(전면 개정 — §1 에 "지금 무엇이 현행인가" 표 신설),
`docs/archives/README.md`(들어온 6개 설명 추가), `docs/docs2/README.md`(남은 3개로 갱신).

**링크 수리**: 옮긴 문서를 가리키던 참조 15군데 + 이동 때 깨진 상대경로 5군데 +
예전부터 깨져 있던 `docs/WORKLOG.md`(실제는 `docs/archives/WORKLOG.md`) 1군데.
마크다운 뿐 아니라 파이썬 주석(`training/scripts/dg5f_status.py`)과
C# 주석(`Dg5fGraspLiftSpec.cs`)까지 고쳤다.

### 돌려본 것과 결과

| 명령 | 결과 | 판정 |
|---|---|---|
| 마크다운 88개의 상대링크 전수 검사 | 깨진 링크 **0개** | 통과 |
| 옮긴 6개의 옛 경로가 md/py/cs/yaml 에 남았는지 | 남은 곳 **0개** | 통과 |

검사 스크립트는 임시로 돌린 것이라 리포에 남기지 않았다.

### 사용자가 결정한 것

- `docs/` 에서 안 읽어도 되는 문서를 아카이브로 보낸다 (요청).

### 막힌 것

없음.

### 남겨 둔 것 (이번에 손대지 않음)

- 리포 루트 `README.md` 에 **낡은 서술이 많다** — "현재 상태 / 알려진 이슈"가
  2026-07-20 기준이고, `training/` 을 아직 `DG5FGraspPointReach` 로 설명한다.
  이번에는 **문서 경로와 관련된 줄만** 고쳤다. 전면 개정은 별도 작업이다.

### 커밋

- `dfb2d76` docs: docs/ 정리 — 구세대 문서 6개 archives 로, 문서 입구 전면 개정
- 이 해시를 적은 후속 커밋이 하나 더 있다 — `origin/main` 으로 푸시했다.

## 다음에 할 일

1. **이 체계를 실제로 한 번 써 본다.** 다음 대화를 열 때 사용자가 상황 설명 없이
   "claudeDocs 읽고 이어서 해" 라고만 해도 되는지 확인한다. 안 되면 `README.md` 의
   읽는 순서를 고친다. ([`BACKLOG.md`](../BACKLOG.md) C-2)
2. **B-1 — 물체별 사람 상수(`place`) 없애기 학습 검증.** 강화학습 쪽에서 가장 우선순위가
   높은 실험이다. 메커니즘은 이미 구현돼 있고 기본값만 아직 옛날 방식(`palm`)이다.
   착수 전에 [`docs/SUPERDEX_POC_PLAN.md`](../../docs/SUPERDEX_POC_PLAN.md) §0 의
   "workspace 스폰" 절을 먼저 읽는다.
3. **리포 루트 `README.md` 전면 개정** — docs 정리를 하다 발견했다. 낡은 상태
   설명이 남아 있어 새 컴패이 그대로 따라 하면 막힌다(원칙 2). ([`BACKLOG.md`](../BACKLOG.md) C-3)
4. **11월 전시가 기한이 있는 유일한 묶음이다.** A-1 ~ A-6 중 A-3(팔 경로를 만들고 저장하고
   실행하는 방법)이 다른 항목의 앞단이라 먼저 정해야 한다.

## 이어받는 사람이 밟을 함정

- **파이썬 환경이 두 개다.** `superdex/` 작업은 `superdex/.venv/`(3.12), 나머지는 3.10.11.
  섞으면 바로 import 에러가 난다.
- 오늘 커밋된 웹캠 작업은 **실제 손으로 녹화해서 확인한 것**이고, 카메라 고르기 창은
  사용자 환경에서 한 번 더 확인이 필요하다.
