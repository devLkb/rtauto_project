# 2026-09-23

전날: [2026-09-22](2026-09-22.md)

---

## 세션 1 — 발표 자료에 UR16e ↔ URSim 추가, 프로젝트 이름 변경

### 한 일

1. **발표 자료(PPT)에 UR16e ↔ URSim 내용 추가** — 사용자 제공
   `피지컬_트윈_AI_개발_진행_현황_20260922_최종.pptx`(7장, 리포 밖 `Downloads`)에
   슬라이드를 추가했다. 내용 출처는 `README.md` "UR16e 팔 디지털 트윈" 절,
   `arm/README.md`, `arm/ur_rtde_bridge.py`, [2026-09-21](2026-09-21.md)·
   [2026-09-22](2026-09-22.md) 기록, `BACKLOG.md` A-1·A-8.
   - 02 전체 진행 현황: "04 UR16e ↔ URSim 디지털 트윈(완료)" 추가, 기존 04 → 05
   - 07 UR16e ↔ URSim 디지털 트윈(흐름 4단계), 08 연동 결과 영상(사용자 녹화
     `bandicam 2026-09-23 09-08-38-356.mp4`를 반복 재생 GIF로), 09 URSim 검증 결과
   - 결과 파일: `Downloads\Cobo-Sight_개발_진행_현황_20260922_최종_URSim추가.pptx`(10장).
     PowerPoint로 이미지 뽑아 배치 확인함, 파일 검사 통과.
2. **URSim 도커 실행** — `arm/run_ursim.ps1 -Detach`, 이미지 5.25.2. 포트
   30001~30004·29999·6080 열림 확인함. 펜던트 전원 ON·Remote Control 전환은 안 함.
3. **프로젝트 이름 변경: "피지컬 트윈 AI" → "Cobo-Sight"** (사용자 지시). PPT 10곳,
   `CLAUDE.md`·`README.md`·`docs/SIM2REAL_ROADMAP.md` 각 3행의 "프로젝트 이름" 줄을 고침.
   리포 안에 옛 이름은 이 세 곳 말고 없었다(grep 확인).

### 사용자가 결정한 것

- 프로젝트 이름은 **Cobo-Sight**. 저장소 문서 전체에서도 바꾼다.

### 다음에 할 일

1. PPT 08번 부제는 영상 방향(펜던트→Unity인지 반대인지)을 몰라 방향 없이 썼다 —
   사용자가 알려 주면 고친다.
2. URSim 컨테이너(`ursim`)가 떠 있을 수 있다. 끌 때:
   `powershell -ExecutionPolicy Bypass -File arm/run_ursim.ps1 -Stop`.

### 커밋

- `e14f575` `docs: 프로젝트 이름을 Cobo-Sight로 변경 + 2026-09-23 세션 기록` — `origin/main`까지 push 완료

---

## 세션 2 — 여러 물체 중 대상 고르는 규칙 고치기 (BACKLOG A-9)

### 사용자가 결정한 것

- **손목 카메라 장착값 재기(D-7)는 오늘 못 한다.** 브라켓이 아직 없어서 카메라를 손목에
  달 수 없고, 장착값은 **달고 나서 재는 값**이라 미리 받을 수치도 없다. 재는 도구를
  미리 만드는 것도 오늘은 안 하기로 했다 — "로직 개선만 해 일단".
- 오늘은 A-9(대상 고르는 규칙)만 한다.

### 설계 (착수 시점)

- 고르는 규칙을 `arm/target_selection.py` 로 따로 뺀다 — `approach_object.py` 는
  "계산 안 하고 순서대로 부르기만" 이 원칙이고, 떼어야 카메라 없이 시험할 수 있다.
- ① 1등이 거절되면(안전 검사 / 관절각 풀기) 2등·3등으로 넘어간다.
- ② 아까 고른 물체가 아직 보이면 그걸 유지하고, 다른 게 `RTAUTO_TARGET_SWITCH_MARGIN_M`
  (기본 3 cm)보다 더 가까워야 바꾼다.
- ③ 이미 있는 `Tracker` 로 번호를 붙인다. 팔이 움직이면 번호를 새로 매긴다
  (`segment_objects.Tracker.reset()` 주석 — 장착값 재기 전까지의 규칙).
- 왜 골랐는지·건너뛴 후보의 거절 이유를 터미널과 `--show` 화면에 찍는다.

### 한 일

| 파일 | 변경 |
|---|---|
| `arm/target_selection.py` (새 파일) | 고르는 규칙 전부. `rank()` 순서 정하기, `choose()` 거절되면 다음, `TargetPicker` 반복 동안 기억 |
| `arm/approach_object.py` | `observe()` 는 후보만 모으고 번호를 붙인다. `run_once()` 가 후보마다 "갈 수 있나"(설 자리 + 관절각)를 검사하며 고른다. 팔이 실제로 움직이면(`executed`) 번호를 버린다. 터미널에 "고른 이유"/"건너뜀" 줄, `--show` 에 빨간 테두리·`#번호` |
| `config/rtauto_config.py`, `.env.example` | `RTAUTO_TARGET_SWITCH_MARGIN_M`(0.03), `RTAUTO_TARGET_HOLD_MISSING_FRAMES`(3) — 둘 다 잠정값 |
| `arm/tests/test_target_selection.py` (새 파일) | 시험 19개. 실제 `Tracker` 로 여러 장면 흘리기, `run_once()` 가 2등으로 넘어가는지 |
| `arm/README.md`, `docs/FESTA_PREGRASP_PLAN.md` | 규칙 설명 갱신 |

관절각 풀기는 **팔에 명령 연결이 있을 때만** 부른다. 연결이 없으면 `ArmMover.plan()`
이 어차피 실패를 돌려주는데(`prepose_to_joints.py:650`), 그걸 거절로 세면 모든 후보가
"거절"로 잘못 찍히기 때문이다. 이전 동작과 결과는 같다(연결 없으면 관절각 확인 못 함).

### 실물로 확인하다 찾은 것 — 인식이 깜빡이면 대상이 바뀐다

처음 구현(①②③만)을 실물 D405 `--look --watch --target cup` 으로 돌리니, 확신 40 %
안팎의 컵(#2)과 #4 가 **한두 장면씩 인식에서 빠졌다 돌아왔고, 빠질 때마다 대상이
바뀌었다.** 무인 반복(`--move --watch --yes`)이면 깜빡임 한 번에 팔이 다른 물체로 간다.
→ 규칙 ④ "잠깐 안 보여도 3장면까지는 기다린다" 를 추가했다.

### 직접 돌려서 확인함

| 확인한 것 | 결과 |
|---|---|
| 시험 | **219개 전부 통과** (어제 200 + 오늘 19) |
| 실물 D405 `--look` | 후보에 `#1`~`#4` 번호가 붙고 "고른 이유" 가 찍힌다 |
| 실물 D405 `--look --watch --target cup`, 규칙 ④ **전** | 15장면 동안 대상 **5번** 바뀜 |
| 같은 배치, 규칙 ④ **후** | 23장면 동안 **2번** 바뀜 — 한 번은 4장면 연속 안 보여 포기, 한 번은 돌아온 #2 가 3.5 cm 더 가까워서. 둘 다 규칙대로. 2.6 cm 차이일 땐 유지한 것도 봤다 |
| URSim `--plan --ip --target cup` | 컵 3개를 **순서대로 전부 시도**하고 각각 "작업 공간 상자 밖" 이유를 찍은 뒤 안 움직였다 |
| `--show` 그림(파일로 뽑아 봄) | 고른 것 초록 + "이걸로 간다", 건너뛴 것 빨강 + "건너뜀", 나머지 주황 `#번호`, 한글 정상 |

### 확인 못 한 것

- **후보가 받아들여진 뒤 관절각이 안 풀려 다음으로 넘어가는 경우**는 실물로 못 봤다 —
  카메라가 손목이 아니라 책상에 있어(장착값 0) 컵이 전부 작업 공간 밖으로 거절됐다.
  시험(가짜 설 자리 계산)으로만 확인함.
- `--move --watch` 로 **팔을 실제로 움직인 뒤 다시 고르는 흐름**은 안 돌렸다.
- `--show` 에서 물체가 붙어 있으면 `#번호` 글자끼리 겹친다(그림에서 봄). 안 고침.

### 다음에 할 일

1. 브라켓이 나오면 **손목 카메라 장착값 재기(D-7)** — 여전히 유일한 임계경로. 재는
   도구는 아직 없다(오늘 사용자 결정으로 미룸). 브라켓 일정이 잡히면 그 전에 만든다.
2. 카메라를 손목에 단 뒤 `--move --watch --unvalidated-mount-ok` 로 URSim 에서 "움직인 뒤
   다시 고르기" 확인.
3. ~~YOLO 가 찾은 것이면 이름과 상관없이 후보다~~ → 같은 날 사용자 결정으로 기본값을
   cup,bottle 로 제한함(아래 "이어서 2"). 범용 파지로 넘어갈 때 이 제한을 다시 볼 것.
4. 순서 기준이 여전히 거리 하나다(확신은 안 본다) — 확신 36 % 짜리 컵이 1등이 되곤 했다.
   `--plan` 이상에서는 `RTAUTO_MIN_DETECT_CONFIDENCE`(40 %)가 거르지만 `--look` 에선 안 거른다.

### 세션 2 (이어서) — 별도 검토에서 나온 것 고침

다른 검토자(코드 리뷰 에이전트)에게 맡겨 받은 지적과 처리:

| 지적 | 처리 |
|---|---|
| 🛑 **이동이 중간에 실패하면 번호가 안 지워진다** — `move_to()` 는 첫 이동 뒤 마지막 직선 구간이 실패하거나 통신이 끊기면 `executed=False` 를 돌려주는데, 팔은 이미 움직였다. 옛 번호가 다른 물체에 붙어 무인 반복이면 그쪽으로 갈 수 있다 | `move_to()` 를 **부르기만 하면**(예외가 나도) 번호를 버린다(`try/finally`) |
| 사람이 `N` 으로 취소해도 다음 장면에서 같은 물체를 또 고른다 | `TargetPicker.refuse()` — 팔이 움직이기 전까지 그 번호는 "사람이 취소했다" 로 건너뛴다 |
| 아무것도 안 보인 장면은 "기다림" 횟수에 안 센다 | 빈 장면에도 `Tracker` 와 기다림 횟수를 갱신한다 |
| 전부 거절됐을 때 1등이 **관절각에서** 거절됐으면 `--pose-json` 에 "받아들임" 자세가 "거절 기록" 으로 저장된다 | 설 자리 계산에서 거절된 경우만 저장·반환 |
| 기다리는 장면 수가 번호 기억 장면 수(8)보다 작아야 한다는 게 주석뿐 | 시작할 때 어기면 경고 |
| 시험이 불안정했다(19개 중 1개 오류, 한 번 멈춤) | 원인 못 찾음. 검토 시각에 세션 3 이 `eye_in_hand.py`·설정을 고치던 중이라 그 영향으로 추정 — **추정일 뿐**. 고친 뒤 새 시험 25개 **5번 연속**, 전체 239개 **2번 연속** 통과 |

시험 6개 추가(관절각 거절 → 2등으로 이동 / 전부 관절각 거절 시 파일 안 씀 / 이동 실패·예외 시
번호 버림 / 취소한 물체 다시 안 고름 / 빈 장면도 셈). 세션 3 이 남긴 `approach_object.py`
28행 옛 설정 이름(`RTAUTO_D405_MOUNT_VALIDATED`)도 새 방식(`config/d405_tool_camera.json` 의
`status`)으로 고쳤다.

**직접 돌려서 확인함**: 전체 시험 **239개 통과**(2번), 실물 D405 `--look --watch --target cup`
8장면 오류 없음.

**커밋 방식 (사용자 결정: 둘로 나눠 커밋).** 세션 3 의 변경(장착값 파일 분리)이 같은
파일들(`config/rtauto_config.py`, `.env.example`, `arm/README.md`, `docs/FESTA_PREGRASP_PLAN.md`,
`BACKLOG.md`, 이 문서)에 섞여 있었다. 세션 3 이 **아직 파일을 고치는 중**(12:28 수정 확인)이라
그쪽은 건드리지 않고, **이 세션(A-9) 부분만** 마지막 커밋 위에 정확히 끼워 커밋했다 —
세션 3 부분은 작업 폴더에 커밋 안 된 채 그대로 남아 있다. 세션 3 이 끝나면 그쪽에서 커밋할 것.

⚠️ 처음엔 줄 단위로 골라 올렸는데(`git apply --unidiff-zero`), 설정 파일의 `WORKSPACE_MAX_M = tuple(`
한가운데에 끼어 들어가 **파이썬이 읽지 못하는 상태**였다. 커밋할 내용만 따로 풀어 시험을 돌려 보고
찾았다. 파일마다 글자 단위로 끼워 넣는 방식으로 다시 올렸다. 커밋 내용만 풀어 둔 폴더에서
시험 225개 통과를 확인한 뒤 커밋했다(세션 3 의 시험 14개가 빠진 수).

### 세션 2 (이어서 2) — 잡으러 갈 물체 이름을 컵·병으로 제한

**사용자 결정**: "일단은 cup bottle만 하드코딩해서 정해둬". 원칙 1 때문에 코드에 박지 않고
설정 기본값 `RTAUTO_TARGET_NAMES=cup,bottle` 로 넣었다(`config/rtauto_config.py`). 효과는 같다.
`--target` 을 주면 그 값이 우선, `--target all` 이면 제한 없음, 설정을 비우면 제한 없음.
⚠️ 범용 파지 목적과 반대 방향의 **임시 제한**이다.

**직접 돌려서 확인함**: 시험 **248개 통과**(이름 규칙 시험 4개 추가). 실물 D405 `--look`
(`--target` 없이) — YOLO 가 keyboard 86 %·cup 79 %·bottle 76 %·cell phone 49 % 를 찾았고,
**키보드·휴대폰은 빠지고 컵을 골랐다**(병은 "같이 보인 것").

### 세션 2 커밋

- `5897a06` `feat(arm): 여러 물체 중 어느 것으로 갈지 고르는 규칙 (A-9)` — **push 안 함**.
  이 커밋 번호 줄은 세션 3 변경과 같은 파일에 있어 아직 커밋 안 됨(세션 3 커밋 때 같이 들어간다).

---

## 세션 3 — 손목 카메라 장착값(T_tool_camera)을 파일로 분리 + "아직 안 쟀다" 관문

> 세션 2(A-9)와 **같은 시각에 따로 돌았다.** 세션 2 가 고치던 `arm/approach_object.py`·
> `arm/target_selection.py` 는 건드리지 않았다.

### 사용자 지시 (2026-09-23)

- 실제 D405 의 tool0 기준 장착 위치·회전은 **아직 안 쟀다.** 그 때문에 멈추지 말고,
  장착값을 설정으로 빼고 자리 표시용 값으로 전체 경로(카메라 → tool0 → 로봇 밑동)를 완성할 것.
- 장착값이 안 잰 상태면 **실물 이동을 막는 안전장치**를 둘 것. 남의 로봇·다른 카메라 값을
  실측값처럼 쓰지 말 것. 필요한 곳에 `TODO` / `CALIBRATION_REQUIRED` 표시.

### 한 일

이미 있던 것: 좌표 변환(`arm/eye_in_hand.py` — `P_base = T_base_tool x T_tool_camera x P_camera`),
팔 관절각에서 tool0 자세 계산(`tool0_pose_from_joints`), 깊이 → 점(`depth_to_points_cam`),
실물 이동 관문(`require_validated_mount_for_physical_move`). 비어 있던 것만 채웠다.

1. **장착값을 `.env` → 파일 `config/d405_tool_camera.json` 으로 옮김.** 장착값은 PC 가 아니라
   로봇에 딸린 값이라 git 이 추적해야 한다(`.env` 는 다른 PC 로 안 따라간다).
   파일에 `status`·`source`(출처)·`measured_on`(잰 날짜)·`child_frame`(컬러 렌즈 기준) 을 둔다.
   지금 값: 회전 없음·이동 없음, `status: CALIBRATION_REQUIRED`, `source: PLACEHOLDER`.
2. `config/rtauto_config.py`: `D405_MOUNT_PARENT/XYZ_M/RPY_DEG/VALIDATED`, `d405_mount_measured()`,
   `d405_mount_validated()` 삭제 → `D405_TOOL_CAMERA_FILE`(`RTAUTO_D405_TOOL_CAMERA_FILE`) 추가.
   **옛 `.env` 키가 남아 있으면 멈춘다**(정본이 둘이 되지 않게). 사용자 `.env` 에는 옛 키가 없었다(확인함).
3. `arm/eye_in_hand.py`:
   - `status` 3단계 — `CALIBRATION_REQUIRED`(경고, 이동 막힘) / `MEASURED`(경고 없음, 이동 막힘) /
     `VALIDATED`(이동 열림). 가짜 팔(URSim) 예외는 그대로.
   - 회전을 roll·pitch·yaw 또는 **쿼터니언(x, y, z, w)** 으로 적을 수 있다(`quat_to_matrix`, 길이 검사).
   - 파일 검사: `MEASURED`/`VALIDATED` 인데 값이 자리 표시용 그대로거나, 출처·날짜가 없으면 멈춘다.
   - `read_tool0_pose(ip)` — 팔에서 관절각을 읽어 T_base_tool 재료를 준다(`--check --ip` 가 이걸 쓴다).
4. 시험 14개 추가(`arm/tests/test_eye_in_hand.py` `MountFileTests`·`FrameChainTests`).
5. 옛 키 이름을 쓰던 문서 고침: `.env.example`, `arm/README.md`(2곳), `docs/FESTA_PREGRASP_PLAN.md`(2곳),
   `BACKLOG.md` D-7, `superdex/scripts/candidate_pose_generator.py` 주석.

### 확인함 (직접 돌림)

- `python -m unittest arm.tests.test_eye_in_hand arm.tests.test_pregrasp_planner arm.tests.test_prepose_to_joints arm.tests.test_target_selection` → 131개 통과
- `python -m unittest discover -s vision/d405/tests` → 73개 통과
- `python arm/eye_in_hand.py --check` → `상태 : CALIBRATION_REQUIRED ← 아직 안 쟀다` 와 이동 막힘 문구 출력
- `python arm/approach_object.py --help` 정상
- 팔(URSim)·카메라에 붙여 `--check --ip` 는 **안 돌렸다.**

### 남은 것

- `arm/approach_object.py` 28행 설명에 옛 이름 `RTAUTO_D405_MOUNT_VALIDATED=1` 이 남아 있다 —
  세션 2 가 고치는 중인 파일이라 손대지 않았다. 세션 2 커밋 뒤 한 줄 고칠 것.
- `arm/approach_object.read_tool0_pose()` 와 새 `eye_in_hand.read_tool0_pose()` 가 같은 일을 한다 —
  나중에 하나로 합칠 것(approach_object 쪽이 eye_in_hand 것을 부르게).

### 다음에 할 일

1. 브라켓을 달면 D-7 로 장착값을 재서 `config/d405_tool_camera.json` 의 translation_m / rotation /
   source / measured_on 을 채우고 `status` 를 `MEASURED` 로. 실물 검증까지 끝나면 **사람이** `VALIDATED` 로.
2. 위 "남은 것" 두 줄.

### 코드 리뷰 반영 (같은 세션, 별도 리뷰 에이전트)

- 발견: JSON 이 `NaN` 을 받아 주어 **NaN 장착값이 `VALIDATED` 로 관문을 통과**했다(좌표가 전부 nan). → 숫자 검사(유한수만, true/false·글자 거부) 추가.
- 함께 고침: 날짜는 `2026-09-23` 형식만, `parent_frame` 은 읽을 때 검사, 깨진 JSON·`[]` 은 파일 이름과 함께 멈춤, `validated=True`+`measured=False` 조합 금지,
  팔 끝에서 `RTAUTO_D405_MOUNT_MAX_OFFSET_M`(기본 0.30 m, 넉넉히 잡은 상한 — 실측값 아님)보다 멀면 단위 착오로 보고 거절.
- 재실행: arm 시험 4묶음 **142개 통과**(확인함). 커밋은 안 함 — 세션 2 작업과 같은 파일(`.env.example`·`config/rtauto_config.py`·일지)이 섞여 있다.

### 이어서 (13:40)

- 세션 2 가 `5897a06` 으로 A-9 를 커밋했다(세션 3 변경은 빼고). 그 뒤 남은 두 줄 처리:
  `approach_object.py` 28행은 세션 2 가 이미 새 이름으로 고쳤음. `approach_object.read_tool0_pose()` 는 이제 `eye_in_hand.read_tool0_pose()` 를 부른다(중복 제거).
- 재실행: arm 시험 142개, vision/d405 시험 73개 통과(확인함).
- `--check --ip` 를 URSim 에 붙여 보려 했으나 **Docker Desktop 이 꺼져 있어** 컨테이너가 안 떴다 — 안 돌림.

### 다음에 할 일 (갱신)

1. Docker Desktop 을 켠 뒤 `powershell -ExecutionPolicy Bypass -File arm/run_ursim.ps1 -Detach` → `python arm/eye_in_hand.py --check --ip` 로 팔에서 읽은 tool0 자세가 찍히는지 확인.
2. 브라켓을 달면 D-7 로 장착값을 재서 `config/d405_tool_camera.json` 을 채우고 `status` 를 `MEASURED` 로(검증 뒤 사람이 `VALIDATED`).

### 세션 3 커밋

- `f1eacd3` `feat(arm): 손목 카메라 장착값(T_tool_camera)을 파일로 분리 + 미측정 관문` — **push 안 함**.

### URSim 확인 (Docker Desktop 켠 뒤)

- `arm/run_ursim.ps1 -Detach` → `python arm/eye_in_hand.py --check --ip` **돌려서 확인함**:
  팔에서 읽은 tool0 자세 (-0.1845, -0.4397, +0.2784) m, 컨트롤러 활성 TCP 와 같음(URSim 기본 TCP 가 tool0).
  카메라 앞 10 cm 점 → 로봇 기준 (-0.1820, -0.4372, +0.1784) m, CALIBRATION_REQUIRED 경고·이동 막힘 문구 함께 출력.
- URSim 컨테이너는 띄워 둔 상태. 끌 때: `powershell -ExecutionPolicy Bypass -File arm/run_ursim.ps1 -Stop`.
- 다음에 할 일은 위 "다음에 할 일 (갱신)" 2번(브라켓 달고 D-7)만 남았다.

### 사용자 결정 (2026-09-23 오후) — 브라켓은 새로 만들어야 해서 오래 걸릴 수 있다

- 기다리는 동안 **책상 고정 카메라로 먼저** 간다(사용자 선택). 카메라가 "로봇 밑동 기준 어디에 있나"(T_base_camera)를 한 번 재면
  브라켓 없이도 로봇 기준 좌표가 실제로 맞게 나온다. 손목에 달면 장착값 파일만 바꿔 끼운다.
- 착수 설계: ① 장착 방식 설정 `RTAUTO_D405_MOUNT`(wrist|fixed) + 고정용 파일 `config/d405_base_camera.json`
  ② 재는 도구 `arm/calibrate_camera_mount.py` — 체커보드를 찍으며 팔 자세와 함께 기록 → OpenCV `calibrateHandEye` 로 풀기.
  고정 카메라일 때는 체커보드를 손/팔 끝에 테이프 등으로 단단히 붙이고(보드가 붙은 자리는 몰라도 된다) 팔을 여러 자세로 옮긴다.
  같은 도구가 나중에 손목 카메라(wrist)도 잰다. 결과는 `MEASURED` 까지만 적고 `VALIDATED` 는 사람이 적는다.

### 사용자 결정 정정 (같은 날, 착수 직후)

- 위 "책상 고정 카메라로 먼저" 는 **취소.** 사용자가 의미를 되물었고, 장단점(D405 는 7~50 cm 근거리용이라 책상 배치가 까다롭고,
  최종 구성인 손목 카메라로 가면 결과 대부분을 다시 확인해야 함)을 설명한 뒤 **"카메라 일은 멈춤"** 으로 결정했다.
- 코드는 한 줄도 안 바꿨다. 카메라 관련(장착값 재는 도구 포함)은 **브라켓이 나올 때까지 보류.** 브라켓은 새로 제작해야 해서 오래 걸릴 수 있다.

### 사용자 요청 (같은 날) — 텔레옵 MediaPipe 정확도

- "다른 건 다 잘 되는데 **엄지와 중지가 잘 안 움직인다.** 기존 코드·관례 안 지켜도 되니 해결해 봐." 조사 착수.
- 사용자 증상(Unity 화면): ① **중지가 좌우로 안 움직인다** ② 손바닥을 쫙 편 상태인데 **엄지가 앞으로 나온다** — "앞으로 와야 할 때와 옆에 있어야 할 때" 구분이 안 됨.
- 로그 `vision/dg5f/logs/vision_dg5f_20260922_153958.csv`(손 잡힌 3,703장면): 중지 굽힘은 끝까지 움직임(0→97°). **중지 좌우(middle_abd)는 항상 0** — `dg5f_angles.py` 표에서 일부러 꺼 둔 채널(gated)이다.
- **브랜치 `fix/teleop-thumb-middle`** (사용자 지시: 새 브랜치에서). main 폴더는 다른 세션이 쓰고 있어 **별도 폴더 `D:/workspace/KDT_1_AX_rtauto-teleop`(git worktree)** 에서 작업. `.env` 는 main 폴더에서 복사함.
- 원인 확인(숫자): Unity 는 파이썬 각도를 그대로 따른다(1_1 상관 0.91). ① 중지 3_1 은 `dg5f_angles.py` 게이트로 항상 0. ② 엄지 대향(1_2)은 "엄지끝~새끼 뿌리 거리" 방식인데 **손을 편 녹화(2026-09-16)에서 로봇 1_2 가 -31~-47°**(0 이어야 함) — 7월에 맞춘 기준값 1.33 보다 실제 편 손 거리가 0.83~0.99 로 짧다.
- 커밋(브랜치) `dff8e9d`: 중지 게이트 해제 + 자세 녹화 도구 `vision/dg5f/record_hand_poses.py` + 분석 도구 `analyze_hand_poses.py`. 시험 77개 통과(확인함).
- 다음: 사용자가 자세 녹화 → 분석해 엄지 계산식을 새로 고른다.

### 카메라 선택 빠르게 (사용자 요청: "카메라 자동 말고 기존 리스트만 빠르게")

- 브랜치 `fix/teleop-thumb-middle` 커밋 `dcfd505`: 선택 창(ask)이 저장된 목록(`vision/dg5f/camera_caps_cache.json`)을 카메라를 열지 않고 바로 띄운다. "다시 찾기" 버튼(터미널 `r`)일 때만 예전 방식.
- 실제 웹캠 2대로 **직접 재서 확인함**: 목록이 뜨기까지 예전 4.62초 → 0.25초. 시험 82개 통과.
- ⚠️ `camera_caps_cache.json` 은 git 이 안 따라간다 — 브랜치 폴더로 main 폴더 것을 복사해 둠.

### 창 개선 (사용자: "녹화 창이 너무 크다", "q 로만 나가게 한 이유? X 로도 꺼지게")

- 브랜치 커밋 `89b14cb`: `vision/cv_window.py` 신설 — OpenCV 창 14곳(vision/dg5f·d405·zed, arm/approach_object)이 X 로도 끝난다. 녹화·프로브 창은 `RTAUTO_VISION_PREVIEW_WIDTH`(기본 1280) 폭으로 뜬다.
- 확인함: 실제 창을 띄웠다 닫아 "보이다가 사라짐" 판정이 되는 것 확인. 시험 87개(dg5f)·73개(d405)·arm 시험 통과.
- 첫 녹화 `logs/handposes_20260923_142449.csv`(브랜치 폴더)는 중단된 것 — 손이 한 번도 안 잡힘(0/131). **다시 녹화 필요.**

### 창 수정 (사용자: "녹화 창이 너무 크다", "q 말고 X 로도 꺼지게")

- 브랜치 커밋 `89b14cb`: `vision/cv_window.py` 신설 — 보이던 창이 사라지면 "사람이 닫았다" 로 판정. OpenCV 창 14곳(vision/dg5f·d405·zed, `arm/approach_object.py`)에 적용. 녹화·프로브 창은 `RTAUTO_VISION_PREVIEW_WIDTH`(기본 1280) 폭으로.
- 확인함: 실제 창을 띄웠다 닫아 판정이 False→True 로 바뀌는 것, dg5f 시험 87개·d405 73개·arm 시험 통과.
- 첫 녹화 `handposes_20260923_142449.csv`(브랜치 폴더)는 손이 한 번도 안 잡힘(131장면 중 0) — 창이 커서 멈춘 시도로 보임. **다시 녹화 필요.**

### 다음에 할 일 (텔레옵)

1. 사용자가 `record_hand_poses.py` 로 다시 녹화 → `analyze_hand_poses.py` 로 엄지 계산식 고르기 → `dg5f_angles.py` 엄지 1_1·1_2 매핑 교체.
2. 사용자가 Unity 에서 중지 좌우 확인.
3. 끝나면 브랜치 `fix/teleop-thumb-middle` 를 main 에 합칠지 사용자에게 묻기. `arm/approach_object.py` 는 main 쪽 다른 세션 작업과 겹칠 수 있다(창 X 부분만 바꿈).

### 엄지·중지 고침 (자세 녹화 `vision/dg5f/logs/handposes_20260923_144628.csv` — 브랜치 폴더, 자세 10개 × 약 210장면, 전부 손 잡힘)

- 브랜치 커밋 `09c6e42`: 엄지 1_2 를 "첫 뼈 들림 각 + 손바닥 가로지름" 으로 교체(`THUMB_OPP_METHOD="lift_cross"`, 옛 방식은 `"distance"`). 중지 3_1 0점 5° 보정.
- 녹화로 직접 계산해 확인함(로봇 1_2 중앙값, 옛→새): 붙이기 -43→0° / 벌리기 0→0° / 집게 -39→-58° / 앞으로 -51→-83° / 새끼 뿌리 -95→-92°. 중지: 편 손 0° / 검지 쪽 -12° / 약지 쪽 +9°.
- 시험 94개 통과. **Unity 에서 눈으로는 아직 안 봄** — 사용자 확인 필요.
- 한계: 녹화 한 번(한 사람, 한 카메라, 한 조명). 1_1(엄지 벌림/접힘)은 손대지 않았다 — 녹화상 붙이기 +49° / 벌리기 -22° 로 이미 갈렸다.

### main 에 합침 (사용자 지시)

- 사용자 확인(Unity): "잘 따라 한다". 다만 **집게 같은 자세는 손을 카메라 정면으로 보여 줘야 잡힌다** — 웹캠 1대라 손이 옆을 보면 엄지 들림이 안 보인다. 남은 약점으로 기록.
- `c3d1008` merge(`fix/teleop-thumb-middle` → main, **push 안 함**). main 의 다른 세션 미커밋 변경(목표 물체 이름 RTAUTO_TARGET_NAMES 등)은 stash→merge→pop 으로 **그대로 보존**, 충돌 없음. 합친 뒤 main 에서 시험 통과(arm / dg5f 94 / d405 73, 확인함).
- 브랜치 폴더 `D:/workspace/KDT_1_AX_rtauto-teleop` 는 남겨 둠 — 녹화 원본 `vision/dg5f/logs/handposes_20260923_144628.csv`(git 미추적)가 거기 있다. 지우기 전에 main 쪽 logs 로 옮길 것.

### 다음에 할 일 (텔레옵, 갱신)

1. 손 방향에 덜 민감하게: 손이 옆을 볼 때 엄지 들림을 못 읽는 문제(웹캠 1대 한계). 후보 — world 점 쓰기, 손 방향이 나쁠 때 마지막 값 유지, 카메라 2대.
2. 녹화 원본을 main 폴더 logs 로 옮기고 worktree 정리.

- 정정(같은 날): 위 "다음에 할 일" 1번의 후보 중 **world 점 쓰기는 뺀다.** 같은 녹화의 `analyze_hand_poses.py` 결과에서 world 점이 오히려 못 갈랐다(엄지 첫 뼈 들림 각, 가르는 힘 — 붙이기 vs 앞으로: 사진 점 3.06 / world 0.75, 붙이기 vs 집게: 2.19 / 0.00). 남은 후보: 손이 옆을 볼 때 엄지 값 유지, 카메라 2대.
- 사용자(같은 날): "카메라 n대 해 보니 생각보다 할 것도 많고 성능이 별로" → **카메라 여러 대 후보도 뺀다.** 남은 후보: 손이 옆을 볼 때 엄지 값 유지(미착수, 사용자 결정 대기).

### 엄지 — 손이 옆을 볼 때 (사용자 선택: "만들어 보기", 브랜치 `fix/teleop-thumb-hold`, 폴더 `KDT_1_AX_rtauto-teleop`)

- 정면 녹화를 다시 보니 손바닥 정면도(1=정면)가 모든 자세 중앙값 0.94~0.98 — **옆을 본 장면이 거의 없어 사용자가 말한 실패가 데이터에 없다.** 또 "직전 값 유지" 만으로는 옆을 본 채 시작한 집게를 못 알아본다(사용자에게 설명함).
- 계획: ① 엄지끝~검지끝 거리(정면 녹화: 집게 0.16 / 편 손 0.68)로 집게를 따로 알아보기 ② 손이 거의 완전히 옆일 때만 엄지 앞뒤 값 유지. 먼저 옆으로 돌린 녹화가 필요.
- `cc520cf`(브랜치): `record_hand_poses.py --set side` — 옆으로 돌린 자세 6가지. 사용자 녹화 대기.

- 옆 녹화 `vision/dg5f/logs/handposes_20260923_152545.csv`(브랜치 폴더, 6자세 × 약 200장면) 분석: **지금 방식은 옆을 봐도 버틴다** — 거의 옆(정면도<0.3)에서 편 손 0.00 / 벌리기 0.00 / 집게 1.00 / 앞으로 0.90. 그래서 "직전 값 유지" 는 안 넣음.
- 약점은 집게가 방향마다 다른 것(정면 0.65, 장면마다 0.26~0.81 / 옆 1.00). `a20b161`: 엄지끝~검지끝 거리로 집게를 0.75 로 모음 → 정면·옆·돌리는 중 모두 0.75(확인함, 시험 97개 통과). **Unity 에서는 아직 안 봄.**

### main 에 합침 — 텔레옵 마무리 (사용자: "일반 웹캠으론 이정도가 한계 같다")

- `2a33b1f` merge(`fix/teleop-thumb-hold` → main, **push 안 함**). main 미커밋 파일과 겹침 없음. 합친 뒤 dg5f 시험 97개 통과(확인함). 옆 녹화 원본도 main 폴더 `vision/dg5f/logs/` 로 복사.
- 텔레옵 엄지·중지는 여기서 마무리. 남은 한계: 웹캠 1대. 로봇 엄지가 검지에 덜/더 닿으면 `THUMB_OPP_PINCH_AMOUNT`(0.75)만 조정.
- worktree `D:/workspace/KDT_1_AX_rtauto-teleop` 는 이제 지워도 된다(녹화는 main 쪽으로 복사 끝).

### 정리·push (사용자 지시)

- worktree `KDT_1_AX_rtauto-teleop` 삭제, 합친 브랜치 `fix/teleop-thumb-middle`·`fix/teleop-thumb-hold` 삭제. 텔레옵 녹화·실행 기록(`vision/dg5f/logs/*20260923*`, git 미추적)은 main 폴더로 옮긴 뒤 지움.
- **`origin/main` 에 push 완료** (`08e69c9..2a33b1f`, 커밋 13개 — 세션 2 의 A-9 `5897a06` 포함).
- ⚠️ main 에 **다른 세션의 미커밋 변경이 남아 있다**(`RTAUTO_TARGET_NAMES` 등: `.env.example`, `arm/README.md`, `arm/approach_object.py`, `arm/tests/test_target_selection.py`, `config/rtauto_config.py`) — push 에 안 들어갔다. 이 일지 파일도 미커밋.

- 남은 미커밋(세션 2 의 `RTAUTO_TARGET_NAMES`) 도 사용자 지시로 정리: arm 시험 146개 통과 확인 후 `e84c829` 커밋. 이 일지 커밋과 함께 push — **main 에 남는 변경 없음.**
